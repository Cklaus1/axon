# Commits that changed formatting only, and should not own a line in `git blame`.
#
# Nothing reads this file automatically. To use it:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub's blame view honours it without that step. Entries must be FULL 40-char
# SHAs — git errors out on an abbreviated one rather than skipping it.
#
# Only add a commit here if it is provably layout-only. A commit in this list is
# invisible to the next person bisecting a blame, so a behaviour change hidden in
# one is a behaviour change nobody can find.

# style: one formatting sweep, now that both sides read the same rustfmt
# `cargo fmt --all` across 37 files, nothing else in the commit. Layout-only was
# checked, not assumed: with all whitespace stripped, before/after differ only in
# trailing commas on broken lines, `use`-list reordering, and braces around a
# match arm that no longer fit. Suite was 1866 passed / 0 failed either side.
29802068333c71877944862e770ea01f81773a44
