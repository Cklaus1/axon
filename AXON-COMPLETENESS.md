# Axon completeness

**GENERATED from `AXON-COMPLETENESS.json` by `scripts/completeness.py`. Do not edit this file — edit the JSON.**

`production proof` asks whether the PRODUCTION path is proven to use a thing, not whether it exists and has tests. Those come apart constantly, and every row claiming a proof cites evidence the generator checks for.

A `?` is an honest answer and is more useful than a guess: it marks work whose state nobody has established.

## compiler

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| lexer / parser | ✓ | ✓ | ✗ | ✓ |  |
| type inference / checker | ✓ | ✓ | ✗ | ✓ |  |
| interp/codegen parity | ✓ | ✓ | ~ | ✓ | AUDITED, and softer than this row read. Nothing asserts the '52 harnesses' figure: there are 54 files and parity_all's floor is 40, so 4-9 harnesses can silently flip to SKIP and still pass. ~19 harnesses treat a per-case NATIVE BUILD FAILURE as a skip with stderr to /dev/null — so a codegen regression in the builtin under test reads as 'skipping'. dict_parity and arr_reduce_parity were fixed to FAIL on that; the fix was never propagated. Cores are strong; the classification of non-results is where green means less than it reads. |
| single-file diagnostics | ✓ | ✓ | ✗ | ✓ |  |
| MULTI-FILE source identity | ✗ | ✗ | ✗ | ✗ | Span is (start,end) with NO file id. REPRODUCED: an error at lib.ax:14 reports as main.ax:6:112, in a 5-line file. scripts/diagnostic_location_gate.sh is RED on HEAD and NOT yet wired into gate.sh. Design landed: ~110 edits, and the HARM is removable first without touching Span, by making SourceMap::line_col refuse instead of clamp. |
| import resolution determinism | ~ | ? | ✗ | ~ | load_use_decls searches AXON_PATH; the resolved path is not part of any digest, so the same source can mean different programs |
| crate axon-rt | ✓ | ✓ | ✗ | ✓ | ASSESSED. Not a Cargo dependency of anything: axon-core SHELLS OUT to `cargo build -p axon-rt` at link time, so the link is by convention, not by the dependency graph — removing the crate breaks nothing at cargo check and fails only at `axon build` link time. Its execution proof is the parity suite, which runs only under gate.sh --strict and never in CI. |
| crate axon-surface | ✓ | ✓ | ✗ | ~ | ASSESSED. The LIBRARY is production (called from main.rs at three sites). The axon-surface BINARY has no caller. Phase 10 is the only phase with no spec/ file. |

## authority

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| approval binds entry file | ✓ | ✓ | ✗ | ✓ |  |
| approval binds IMPORT GRAPH | ✗ | ✗ | ✗ | ✗ | DEMONSTRATED: approve main.ax, then rewrite an imported module to write:[/] net:[*] exec:any — the approval still validates. End-to-end deploy exploitability NOT established. |
| ast review shows import origin | ✗ | ✗ | ✗ | ✗ | imported fns and their @[contained] caps are listed as the entry file's own, with no origin marker |
| @[contained] capability enforcement | ✓ | ✓ | ~ | ✓ |  |
| policy adapter strict parsing | ✓ | ✓ | ✓ | ✓ | parse_strict verified sound at every depth (decoded keys, recurses maps AND seqs). But the adapter AUTHENTICATES NOBODY: req["principal"] is compared to the operator's --principal and nothing binds a request to its claimed identity — no signature, token, or channel credential. |
| grant / snapshot binding | ~ | ✗ | ~ | ~ | AUDITED, CONFIRMED BY READING: the adapter builds `current` from req["snapshot_id"] verbatim with files:[], and StaleSnapshot is string equality against it — so the requester chooses the value the check compares. The grant's pin is on the adapter's command line (readable via /proc). The module doc forbids deriving the GRANT from the request; the same tautology sits on the other side. Also: `cortex repair` builds grant.principal from the acting principal and pins to a snapshot taken one line earlier, so both checks are structurally unreachable there. |
| path authority covers all actions | ~ | ✗ | ✗ | ✗ | authorize returns early when !requires_write_authority(), BEFORE the traversal/policy-file/prefix checks. Inspect then reads an arbitrary path and hands the body to the model; RunCheck runs the interpreter on an arbitrary path. Both are caller-path-controlled with no path check. |
| durable pre-effect authorization receipt | ✗ | ✗ | ✗ | ✗ | ActionAllowed is pushed into an in-memory Episode the adapter drops at exit; cortex repair serialises it only in its final --json, after every effect. No durable record shows authorization PRECEDED an effect. |
| safety gate verdict readability | ✓ | ✓ | ✓ | ✓ | FIXED and gated. interp.rs `Ok(_) => 0` made Unit/Str/Option/Result indistinguishable from Bool(true); a Critical deploy whose simulate, stress and assert_deployable all returned Err("...FAILED") reported status:deployed, exit 0, with all four gates listed in stages_run. Now blocks. Verified across the whole class (str, f64, Option::None/Some, Result::Ok/Err block; bool true and i64 0 still pass). scripts/gate_verdict_is_read.sh wired into gate.sh. |
| axon verify exit code | ✓ | ✓ | ✓ | ✓ | FIXED. cmd_verify exits 2, not 0, when the prover is not linked (the default build) — proven on a program whose @[verify] bound is false for every input, where `axon verify && axon deploy` previously proceeded. The change broke AXON_REFERENCE.md's 'exits 0' text; the drift gate caught it and the reference was regenerated. |
| crate axon-attest | ✓ | ✓ | ✗ | ✓ | ASSESSED. verify_report is genuinely sound — HMAC recomputed, constant-time compare, key required with no silent degrade, hw_root refused rather than trusted. What is MEASURED is bytes at a caller-supplied path at measure time; nothing binds the measurement to the running process, so 'this stack is attested' is asserted, not measured. measure_extended advertises four components and covers at most three distinct artifacts. |
| crate axon-intent | ✓ | ✓ | ✗ | ✓ | ASSESSED, and the strongest crate here: acceptance.rs drives the real binaries end-to-end and r22 is wired into gate.sh. But the token is an UNKEYED sha256 chain — anyone who edits the program recomputes all three digests and produces a token that verifies. Tamper-EVIDENT against accident, not forgery. program_digest covers the entry file only, so the import graph is unbound. axon-os's verifier does not check `schema` while axon-intent's does: the ENFORCEMENT side is weaker than the producer. |
| crate axon-os | ~ | ✗ | ✗ | ~ | AUDITED. verify_approval is an UNKEYED sha256 over public inputs, presented to the operator as 'someone signed this and the signature does not hold' — anyone who can write <job>.approval mints a valid token. `axon-os verify` prints 'intact' for a fully forged record (the chain is seeded from the record's OWN stored manifest_digest, never compared to the .axjob). The run record holds ONE event whose caps_used is the CEILING, advertised as N events of observed activity. The signed Budget line gates nothing: only `calls` is propagated and even that is debited by no ordinary builtin. scan_effects returns Label::Internal in BOTH branches, so the confidentiality denial states a fact about the program derived from a constant. |
| crate axon-vm | ~ | ✗ | ✗ | ~ | AUDITED. `run --quorum` mints an unpredictable run_id with no --run-id flag, then filters votes by it — approvals are always 0, so the gate is unsatisfiable and reports it as 'insufficient approvals' rather than as unusable. Fail-closed, so not a safety hole. attest with no --verify-digest sets ok:true having compared nothing; key_source (operator vs ephemeral — 'the whole security story' per its own comment) is stderr-only and absent from the JSON. |
| ast review covers impl methods | ✓ | ✓ | ✓ | ✓ | FIXED. cmd_ast_review walks Item::ImplBlock and each fn carries its origin (owner in JSON, [method of Agent] in the human line). Mutation-verified at integration: reverting main.rs alone turns the test red with 'no entry for phone_home — impl methods are invisible'. OPEN: the schema stayed /2 though the POPULATION of fns changed; and gates implemented as impl methods are still invisible to redteam/deploy. |

## cortex

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| repair control loop | ✓ | ✓ | ✓ | ✓ |  |
| localization (Ochiai) | ✓ | ✓ | ~ | ✓ | 31.5% top-1 sole, 89.2% top-3, 0% absent over 111 trials |
| adjudication cannot be gamed | ✓ | ✓ | ✓ | ✓ | 0 false successes / 67 adversarial trials; grader unpatchable at 3 layers; exit 26 refuses a check that already passes |
| workspace restoration on failure | ✓ | ✓ | ✓ | ✓ | 64/64 completed adversarial runs restored; 16/16 out-of-scope unchanged |
| candidate / proposal identity | staged | ✗ | ✓ | ✓ | driver patch verified on a copy but NOT APPLIED — held until the real-model run ends. Analyser fails closed meanwhile. |
| repair action vocabulary | ~ | ✓ | ✗ | ✓ | 4 actions; only whole-symbol-body replacement can edit. No finer-grained or multi-site repair. |

## evidence

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| no positional joins in reporting | ~ | ✓ | ✓ | ✓ | fixed in the analyser and the doc gate; a repo-wide audit found the class also in axon-core span handling |
| docs join by metric identity | ✓ | ✓ | ✓ | ✓ | 4 mutations caught incl. a vacuity guard; covers 2 READMEs, CLAUDE.md and locate.rs |
| absent-vs-zero coercions | ~ | ? | ~ | ~ | several fixed (verdict identity, failing_tests fail-open, unwrap_or_default on the success path); repo-wide audit lane open |
| env var / reference drift gates | ✓ | ✓ | ✓ | ✓ | gated in BOTH directions; caught two vars documented as enforced and read by nothing |
| crate axon-audit | ✓ | ✓ | ✓ | ✓ | ASSESSED, and the strongest evidence crate here — worth recording as negative space. A REAL prev_hash chain; keyed mode adds an authenticated TIP file so truncation is caught, FAIL-CLOSED (a missing tip is a failure, because deleting it is what a truncating attacker does next); the downgrade attack is pinned by a named test (a_keyed_verifier_refuses_an_unkeyed_forgery); r28 carries the repo's only real defect-injection control — it corrupts an entry_hash and asserts verify FAILS — and is wired into gate.sh. Production use in axon-core interp and axon-os. CAVEAT, stated by the code itself: UNKEYED mode is tamper-evident against accident only, since a prefix of a valid unkeyed chain is itself valid. NOTE: it emits schema "axon-ledger/1" — a name owned by the weaker axon-ledger crate, which has no chain at all. |
| crate axon-ledger | ~ | ✗ | ✗ | ~ | ASSESSED. NOT append-only and NOT tamper-evident, though docs claimed both (now corrected): store.rs has prune/replace_record/rewrite_principals, hash.rs has no prev-hash chain and does not commit to causal_parent, and no read path re-verifies a record_id. Depended on only by axon-signal, which is itself wired to nothing. DEAD CODE: crates/axon-ledger/axon-ledger/ is a full nested duplicate crate, ~1100 lines, not a workspace member, never compiled. |
| crate axon-web | ~ | ✗ | ~ | ~ | PARTIALLY FIXED. /api/safety/status no longer reports ✓ for unevaluated axes: coalition_ok is null with a note, attest_mode rides with the tick, killable renamed run_store_writable, and kill files are counted rather than every directory entry. The test that asserted coalition_ok was a BOOLEAN — which the literal true satisfied — now asserts null-plus-note. STILL OPEN: the message field is built from any prose line containing 'passed', which the artifact under review controls. |
| harness skip classification | ~ | ✗ | ✗ | ~ | Two skip detectors disagree: parity_all judges by `tail -1`, cli_run's harness_skipped scans the last 3 lines. A harness whose skip marker is not last is SKIP to one and PASS to the other — handler_resume_parity and exit_code_parity's catch-all both exit 0 with build-error text last, so total codegen breakage classifies as PASS. Also: `cargo test <filter>` exits 0 when the filter matches NOTHING, which increments the very `ran` counter several harnesses use as their vacuity guard. browser_compute_parity has the correct pattern (require >=1 passed) and it was not generalised. |
| harnesses wired to something | ~ | ✗ | ✗ | ✗ | 16 harnesses are reachable from nothing. Two bite: kernel_enforce_test.sh is a rigorous two-case differential with a negative control and is the ONLY live end-to-end proof of the guest-kernel syscall gate — it runs nowhere; and CLAUDE.md's platform-verification claim cites a GPU leg (gfx_wgpu_render_gate.sh) and a browser-WebGPU leg that are wired to nothing. Several acceptance gates also assert properties by grepping SOURCE COMMENTS — r31 backs 'reordering produces a different digest' with the presence of an English phrase. |

## experimental

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| real-model generation evaluation | ~ | ✗ | ✗ | ~ | 25-trial stratified run IN FLIGHT. Transport quality and invalid-output cost UNMEASURED for this run by construction. |
| authority-discrimination corpus | ✗ | ✗ | ✗ | ✗ | no corpus tests permit-vs-refuse with matched pairs; current adversarial evidence shows no false success, not that authority discriminates. Design lane open. |
| feedback-vs-blind ablation | ✗ | ✗ | ✗ | ✗ | deferred until the stratified run lands; would isolate the retry loop's contribution |
| crate axon-ai | ✓ | ✗ | ✗ | ✓ | ASSESSED. The COMPILER-side integration is compiled by NOTHING: `asi-runtime` has zero occurrences in scripts/ or .github/workflows/, so main.rs:1661, main.rs:7135 and interp.rs:2195 are cfg'd out of every gated and CI build. The integration that IS built and tested is axon-cortex's `ai` feature, wired at gate.sh:132. |
| crate axon-signal | ✓ | ✗ | ✗ | ✓ | ASSESSED. 5,389 lines, 59 internal tests, 13 subcommands — and no caller, no dependent, no gate, no CI. The one crate fully built, fully tested internally, and wired to ZERO execution roots. Its GitHub Action is an uninstalled template pointing at the wrong repository. |

## formal

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| SMT discharge | ~ | ✗ | ✗ | ✓ | SETTLED: inert in the default build. `smt` is opt-in and the cfg(not(smt)) compute_discharged returns an EMPTY Discharged, so no check is elided unless built --features smt. The name-collision hazard (Discharged keys on BARE fn names; impl methods share that namespace, and only free fns are ever proven) is therefore LATENT, not live — and 0 collisions exist in the corpus today. Fails SAFE: the default build checks more. |
| crate axon-certcheck | ✓ | ✗ | ✗ | ✓ | ASSESSED. The CALL SITE is real (six CLI verbs) and the BODY is a no-op by default: policy.rs returns Discharged without parsing or checking anything unless AXON_REQUIRE_CERTS is set. Existence vs integration, in the sharpest form — the production path calls it and it returns before doing work. Its only execution is r23. A cross-crate hook to axon-os admission was specified in a comment and never built. |

## self-improvement

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| learned repair policy | ✗ | ✗ | ✗ | ~ | localization/generation policies are fixed; nothing learns from prior episodes |

## platform

| subsystem | impl | production proof | mutation | docs | gap |
|---|---|---|---|---|---|
| crate axon-domain | ✓ | ✓ | ✗ | ✓ | ASSESSED. Real production call sites in interp and native. Its three end-to-end harnesses (fix_codec.sh, fhir_roundtrip.sh, modbus_roundtrip.sh) are invoked by nothing, and R22's spec ticks all three as done. |
| crate axon-gfx | ✓ | ✗ | ✗ | ~ | ASSESSED. A real wgpu implementation behind the `gfx-wgpu` feature, which is off by default and built by NOTHING. Its sole gate (gfx_wgpu_render_gate.sh) is invoked by nothing, and its name does not match parity_all.sh's *_parity.sh glob, so it could not be picked up even accidentally. This is one of the two legs of CLAUDE.md's platform-verification claim, now corrected. |
| crate axon-gfx-mock | ✓ | ✓ | ✗ | ~ | ASSESSED, and the best-covered platform crate. Real production use: axon-rt/src/gfx_mock_ffi.rs imports it and interp dispatches through it. native_gfx_parity.sh is reached BOTH by a cargo test (cli_run.rs) and by parity_all.sh's glob — verified by caller search after a lane cited a line number that did not hold. Caveat: parity runs only under gate.sh --strict and never in CI. |
| crate axon-guest-init | ✓ | ✗ | ✗ | ~ | ASSESSED. Built only by build-guest-image.sh, which is itself referenced only inside COMMENTS. The I/O half — HTTP/MMDS policy fetch, prctl, seccomp, execve, reap — has zero execution coverage; three unit tests cover the pure decision function. Note this crate is what reads the VM's MMDS policy to set AXON_BUDGET_TOKENS, so it is on the authority path. |
| crate axon-guest-kernel | ~ | ✓ | ✗ | ✓ | The SYSCALL GATE is now proven live and gated: kernel_enforce_test.sh boots the freestanding kernel under Firecracker and shows the openat DENIED (exit 8) when FS is withheld and PERMITTED when granted — a real negative control. It was invoked by nothing until now, on a host where all its prerequisites were present. STILL OPEN: hypercall.rs is an empty TODO stub while main.rs unconditionally prints 'hypercall substrate active', so a boot transcript reads as if it shipped; and AXON_KERNEL.md cites qemu_boot_test.sh as proof of this crate, which that script never references. |
| crate axon-wasm | ~ | ✓ | ✗ | ~ | ASSESSED. Production use is the browser playground build. is_prose_goal/compile_prose are #[allow(dead_code)] with no callers — VERIFIED at lib.rs:51 and :62 — while Cargo.toml advertises that the playground 'compiles a PROSE goal file in-browser'. Its real parity test defaults to SKIPPING unless BROWSER_PARITY=1, so the browser leg of interp/native parity is asserted by no automated run. |

## Where the gaps are

21 of 21 crates are represented (0 explicitly excused). 27 of 52 subsystems have a production proof; 2 are UNKNOWN — not failing, unestablished, which is the state most worth acting on.

Deliberately NOT summarised as a single percentage. One number averages over the axis that matters: a parser at 100% and import-graph approval at 0% do not combine into anything a reader can act on.

