[package]
name = "axon-cortex"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Axon Cortex — typed contracts for the M0/M1 repair-episode slice (CX-00/02/03/10)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
