# R21 — Zephyr application config for the Axon demo.
#
# Minimal: console output (printk) over the board's default UART. No heap, no
# networking — the Axon object is freestanding and only needs a byte sink.
CONFIG_PRINTK=y
CONFIG_STDOUT_CONSOLE=y
