{
  "name": "max_returns_min",
  "intent": "`larger` must return the greater of its two arguments.",
  "broken": "fn larger(a: i64, b: i64) -> i64 {\n    if a > b { b } else { a }\n}\n",
  "reference_fix": "fn larger(a: i64, b: i64) -> i64 {\n    if a > b { a } else { b }\n}\n",
  "visible_test": "@[test]\nfn t_visible() { assert_eq(larger(3, 9), 9) }\n",
  "hidden_test": "@[test]\nfn t_hidden() {\n    assert_eq(larger(9, 3), 9)\n    assert_eq(larger(4, 4), 4)\n    assert_eq(larger(0 - 5, 0 - 2), 0 - 2)\n}\n"
}