{
  "name": "sign_missing_zero",
  "intent": "`my_sign` must return 1, -1, or 0 for zero.",
  "broken": "fn my_sign(n: i64) -> i64 {\n    if n > 0 { 1 } else { 0 - 1 }\n}\n",
  "reference_fix": "fn my_sign(n: i64) -> i64 {\n    if n > 0 { 1 } else { if n < 0 { 0 - 1 } else { 0 } }\n}\n",
  "visible_test": "@[test]\nfn t_visible() { assert_eq(my_sign(0), 0) }\n",
  "hidden_test": "@[test]\nfn t_hidden() {\n    assert_eq(my_sign(5), 1)\n    assert_eq(my_sign(0 - 5), 0 - 1)\n    assert_eq(my_sign(0), 0)\n}\n"
}