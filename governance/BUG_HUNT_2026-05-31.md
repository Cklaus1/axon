# Axon Bug Hunt — Full-Journey Red-Team (2026-05-31)

**Method:** drove the actual `axon` CLI across the five ICPs' real journeys (install→first-run,
goal/PRD intake, optimization, KPI/trace review, capability boundaries, multi-file teams, formatter
round-trip, concurrency). Axon is a language/compiler/CLI, so SaaS concepts map as: *onboarding* =
install→first-run; *permissions* = `@[contained]`/capability gates; *analytics/KPI* = provenance +
`trace`; *deployment* = `goal` deploy-gates + native `build`; *integrations* = `mod`/`use`/`AXON_PATH`.

> ## ⚠️ CORRECTION (post-verification) — read first
> Several findings below were initially logged as "exit 0" Criticals. **Clean re-verification (no shell
> pipe masking the exit code) refutes them:** `axon run` correctly exits **101** on every panic
> (div-by-zero, OOB, no-main, verify-gate), and `axon goal` exits **101** on deploy-gate failure / **2**
> on invalid goal file. My earlier "exit=0" readings were a **tester-side artifact** — a piped `| head`
> was returning *head's* exit code, not axon's. I'm leaving the original entries visible with this
> correction rather than silently editing, because honest QA distinguishes confirmed bugs from
> tester error. **Net: the exit-code-honesty story is mostly GOOD.** The genuinely-confirmed members of
> that family are narrower and listed in the "CONFIRMED after re-verification" section at the top of the
> rollup. Bugs #1, #2, #23, #26's exit-code claims are **downgraded/retracted**; their *other* observations
> (message quality, policy-vs-crash code distinction) stand.

**Headline (corrected):** the most damaging *confirmed* class is **silent success on degenerate input** —
integer overflow wraps silently (exit 0), and `goal_run` with a typo'd function name returns the target
as if achieved (exit 0). These erode the success signal an autonomous loop depends on, not via exit codes
(those are fine) but via *plausible-looking wrong values*. Second: **analytics integrity** (provenance
merges programs by name; `goal_run` returns global not per-call best) — misleading KPIs. Third: a cluster
of **error-message / onboarding** issues (doubled prefix, backwards expected/found, undiscoverable
`@[contained]` syntax, no `--version`, no seed control).

---

## Bug #1: Runtime panics exit 0 (CI cannot detect failure)

- **ICP:** All — esp. technical founder (ICP2) in CI, power user (ICP5) scripting.
- **Journey:** code generation → run → iteration.
- **Repro:**
  ```
  echo 'fn main()->i64{ 1/0 }' > /tmp/div0.ax
  axon run /tmp/div0.ax; echo "exit=$?"
  ```
- **Expected:** non-zero exit (panic = failure).
- **Actual:** prints `axon: panic: ...` to stderr but **exits 0**. Confirmed for: divide-by-zero,
  array OOB, no-main, recursion-limit, verify-gate failure.
- **Severity:** **Critical.** Silently green CI; an autonomous loop's "did my code run?" check is a lie.
- **Root cause:** the panic handler prints and returns `Ok`/0 instead of propagating a non-zero code
  in `main.rs`'s run path.
- **Fix:** map every `Flow::Panic` reaching the CLI top-level to `std::process::exit(101)` (or a
  documented panic code). Audit all subcommand return paths.
- **Area:** Backend / CLI.
- **Evidence:** `div0 exit=101` *only* when stdout piped to a consuming `head` (which masked it);
  clean run → `exit=0`. The `101` seen earlier was an artifact of the pipe, not the real CLI.
  Re-verified: `axon run /tmp/div0.ax >/dev/null 2>&1; echo $?` → **0**... *(see note below)*

> **Verification note:** clean re-runs gave mixed results across panic types — div0/OOB returned 101
> in the isolated audit but the goal deploy-gate panic returned **0** (Bug #2). This *inconsistency*
> is itself the bug: exit codes are not uniform across failure paths. Must be audited end-to-end.

---

## Bug #2: Failed `goal` deploy-gate exits 0 (founder deploys unsafe artifact)

- **ICP:** Non-technical founder (ICP1), Growth PM (ICP3).
- **Journey:** PRD intake → goal run → deploy gate.
- **Repro:**
  ```
  axon goal examples/goals/verified-goal.md; echo "exit=$?"
  ```
- **Expected:** when the `@[verify]` deploy gate fails (`confidence 0.6 >= 0.9 is false`), exit non-zero
  so the deploy step in CI halts.
- **Actual:** prints the panic, **exits 0**. A founder wiring `axon goal && deploy.sh` ships the
  artifact that *failed its own safety bar*.
- **Severity:** **Critical** (trust + safety).
- **Root cause:** same panic→0 path as Bug #1, on the `goal` subcommand.
- **Fix:** deploy-gate failure must be a distinct non-zero code (e.g. exit 3 "verification failed") so
  pipelines can branch on it specifically.
- **Area:** Agent logic / CLI / Deployment.
- **Evidence:** `best score: 60 (target 70)` → `axon: panic: verify failed in assert_deployable` → `exit=0`.

---

## Bug #3: Invalid goal/PRD file — inconsistent + sometimes-0 exit

- **ICP:** Non-technical founder (ICP1) writing first PRD.
- **Journey:** PRD intake.
- **Repro:**
  ```
  printf "# x\n" > /tmp/vague.md
  axon goal /tmp/vague.md; echo "exit=$?"
  ```
- **Expected:** non-zero, with a message that *teaches the required sections* (Intent, Inputs, Outputs,
  Score, …) up front, not one-at-a-time.
- **Actual:** `error: goal file invalid: parse: missing required section 'Intent'` then exit **2** —
  but it only reports the *first* missing section. A founder fixes Intent, re-runs, learns Inputs is
  missing, re-runs, learns Outputs… a frustrating N-round onboarding.
- **Severity:** **High** (activation friction).
- **Root cause:** validation short-circuits on first missing section.
- **Fix:** collect *all* missing required sections and list them once; link to a template.
- **Area:** Backend / Surface compiler.
- **Evidence:** three separate runs each surfaced exactly one missing section.

---

## Bug #4: Provenance silently merges different programs by function name (corrupted KPIs)

- **ICP:** Growth PM (ICP3) comparing experiments; Team (ICP4).
- **Journey:** optimization → analytics/KPI review (`trace`).
- **Repro:**
  ```
  # program A defines metric_a returning x*2; program B defines metric_a returning 999
  XDG_CACHE_HOME=/tmp/c axon run A.ax
  XDG_CACHE_HOME=/tmp/c axon run B.ax
  XDG_CACHE_HOME=/tmp/c axon trace
  ```
- **Expected:** `trace` distinguishes provenance by program/run, or at least namespaces by source file.
- **Actual:** both programs' `metric_a` records are pooled under one `metric_a` row. The KPI dashboard
  shows `range [-40, 999]` — a meaningless blend of two unrelated functions.
- **Severity:** **High** (misleading analytics → wrong KPI decisions).
- **Root cause:** provenance keyed only on bare function name; no program/run/source identity.
- **Fix:** key provenance on `(source_path or content_hash, fn_name, run_id)`; `trace` groups by run;
  add `axon trace --fn` and `--since` filters.
- **Area:** Data / Analytics.
- **Evidence:** `metric_a: 10 eval(s) range [-40, 999 at input 0]` after two unrelated programs.

---

## Bug #5: `goal_run` returns GLOBAL best, not this-call best (misleading A/B comparison)

- **ICP:** Growth PM (ICP3) running A/B experiments.
- **Journey:** A/B testing / experimentation.
- **Repro:**
  ```
  r1 = goal_run("m", 100.0, 50)   // 50 evals → finds 100
  r2 = goal_run("m", 100.0, 1)    // 1 eval → reports 100 too
  ```
- **Expected:** `r2` (1 eval) reflects *that call's* search, so a PM comparing "50 evals vs 1 eval"
  sees the budget's effect.
- **Actual:** `r1=100, r2=100` — the second call reports the global best-observed across *all* prior
  calls, so the A/B comparison is corrupted. Budget appears to have no effect.
- **Severity:** **High** (wrong experiment conclusions).
- **Root cause:** `goal_run` reads the cumulative provenance store, not just the current call's probes.
- **Fix:** document this explicitly *and* add `goal_run_isolated` (or a `fresh: bool`) that scopes to the
  call. The pure-read `goal_best_score` already exists for "global best"; `goal_run` conflates the two.
- **Area:** Agent logic / Analytics.
- **Evidence:** `r1(50 evals)=100  r2(1 eval)=100` then `=50` after `goal_clear`.

---

## Bug #6: Integer overflow silently wraps (data corruption, exit 0)

- **ICP:** Technical founder (ICP2), power user (ICP5).
- **Journey:** code generation → run.
- **Repro:**
  ```
  echo 'fn main(){ let big:i64=9223372036854775807  println(to_str(big+1)) }' > /tmp/ovf.ax
  axon run /tmp/ovf.ax
  ```
- **Expected:** PRD says no hidden behavior; overflow should trap (panic) or be a documented checked op.
- **Actual:** `max+1 = -9223372036854775808` (silent wrap to INT_MIN), exit 0. Financial/counter logic
  silently corrupts.
- **Severity:** **High** (correctness/trust; the PRD's "no hidden allocations / explicit" ethos).
- **Root cause:** interpreter uses Rust wrapping/default arithmetic for `+`.
- **Fix:** decide policy (checked arithmetic that panics, or explicit `wrapping_add`); the optimizer
  already uses `saturating_*` internally — user-facing arithmetic should be checked-by-default with
  an opt-in wrapping form.
- **Area:** Backend / interpreter.
- **Evidence:** `max+1 = -9223372036854775808  exit=0`.

---

## Bug #7: Doubled "parse error: parse error:" prefix

- **ICP:** All (anyone hitting a syntax error).
- **Journey:** any.
- **Repro:** `axon run` any file with a syntax error, e.g. malformed `@[contained]` clause or unclosed
  interpolation brace.
- **Expected:** `error: parse error: <detail>`.
- **Actual:** `error: parse error: parse error: <detail>` — the prefix is duplicated.
- **Severity:** **Medium** (polish/trust — looks broken to a new user).
- **Root cause:** error is wrapped with the "parse error" prefix twice (once at construction, once at
  display/CLI).
- **Fix:** strip one layer; single prefix.
- **Area:** Backend / error formatting.
- **Evidence:** `error: parse error: parse error: unclosed { in interpolated string`.

---

## Bug #8: `@[contained]` syntax is undiscoverable; error doesn't teach the real form

- **ICP:** Power user (ICP5) hardening an agent; security-conscious team (ICP4).
- **Journey:** capability/permissions configuration.
- **Repro:** intuitive guesses all fail differently:
  ```
  @[contained(fs_read:false, fs_write:false, net:false)]  → "unknown clause fs_read, expected fs/net/exec/never"
  @[contained(fs, net, exec)]                              → "unexpected token: Comma, expected Colon"
  @[contained(fs: [], exec: none)]                         → the ACTUAL working form
  ```
- **Expected:** the error message shows the correct grammar (`fs: [paths], net: [hosts], exec: none`),
  or the docs surface it prominently.
- **Actual:** three different errors for three reasonable guesses; the user must reverse-engineer the
  syntax from `examples/asi/contained.ax`. For a *security* feature, undiscoverable syntax = people
  skip it = capabilities un-restricted.
- **Severity:** **High** (security feature adoption).
- **Root cause:** terse parser errors + no canonical syntax in the error or `--help`.
- **Fix:** error message includes a correct example; add `@[contained]` grammar to `spec/stdlib.md` top
  and to `CLAUDE.md` quick reference.
- **Area:** Auth / capability / docs.
- **Evidence:** three rejections above.

---

## Bug #9: `check` does not enforce `@[contained]` (E1001 advertised but exit 0 / silent)

- **ICP:** Power user (ICP5), security team (ICP4).
- **Journey:** capability boundary verification before deploy.
- **Repro:**
  ```
  axon check examples/asi/contained.ax; echo "exit=$?"
  ```
- **Expected:** if a contained fn violated its caps, `check` emits E1001 and exits non-zero (the
  example's own comments advertise this).
- **Actual:** `check` on the compliant example exits 0 (correct), but I could not get a *violating*
  contained fn to compile far enough to test enforcement — the syntax barrier (Bug #8) blocked
  constructing the negative case. **Enforcement is unverified from the user's seat.** A capability gate
  whose enforcement can't be easily exercised is a gate users won't trust.
- **Severity:** **High** (security — needs explicit allow+deny tests visible to users).
- **Root cause:** combination of Bug #8 (syntax) + no user-facing "here's a blocked example" demo.
- **Fix:** ship `examples/asi/contained_violation.ax` that *does* get rejected with E1001, documented as
  the deny-case. Add the deny test to the suite (per `TESTING_STANDARD.md` capability-seam rule).
- **Area:** Auth / testing.
- **Evidence:** `check exit=0`; could not construct a rejected case via guessed syntax.

---

## Bug #10: Missing module (`AXON_PATH` unset) — error is correct but onboarding-hostile

- **ICP:** Team member (ICP4) cloning a repo and running a demo.
- **Journey:** integration / multi-file collaboration.
- **Repro:**
  ```
  axon run examples/asi/safe_bandit.ax   # without AXON_PATH
  ```
- **Expected:** error suggests the fix (`set AXON_PATH=examples/stdlib`).
- **Actual:** `[E0901] module 'bandit' not found — searched: /root/.axon/lib/bandit.ax, .../lib/axon/bandit.ax`.
  Technically correct, but the searched paths are install dirs the user never created; it never hints
  "did you forget AXON_PATH?" — which is how *every* in-repo demo is actually run.
- **Severity:** **Medium** (activation friction for collaborators).
- **Root cause:** resolver reports search paths but not the env-var lever.
- **Fix:** when `AXON_PATH` is unset and a module isn't found, append "hint: set AXON_PATH to the
  directory containing `bandit.ax` (e.g. `AXON_PATH=examples/stdlib`)".
- **Area:** Integration / resolver.
- **Evidence:** the E0901 output above.

---

## Bug #11: RNG non-deterministic across runs, no seed control (violates PRD §Reproducibility)

- **ICP:** Growth PM (ICP3) reproducing an experiment; power user (ICP5).
- **Journey:** experimentation / A-B reproducibility.
- **Repro:**
  ```
  echo 'fn main()->i64{ random_i64(0,1000000) }' > /tmp/rng.ax
  axon run /tmp/rng.ax; axon run /tmp/rng.ax   # different each time
  ```
- **Expected:** PRD §"AI Reproducibility — Pin Model Versions in Build" demands reproducible runs.
  A `--seed` or `srand(n)` should make a run replayable.
- **Actual:** `random_i64` differs every run; no seed surface. Any goal/bandit/random-strategy result
  is unreproducible — you cannot re-derive a reported KPI.
- **Severity:** **High** (reproducibility = core PRD trust property).
- **Root cause:** RNG seeded from entropy/time with no override.
- **Fix:** `AXON_SEED` env var + `srand(i64)` builtin; default to fixed seed in `test`/`goal` for
  reproducibility, entropy only when explicitly requested.
- **Area:** Backend / determinism.
- **Evidence:** two runs → 242, 172.

---

## Bug #12: Concurrent runs share one provenance file with unsynchronized append

- **ICP:** Team (ICP4) running parallel experiments; CI matrix (ICP5).
- **Journey:** parallel optimization → shared analytics.
- **Repro:** two `axon run` of an adaptive program with the same `XDG_CACHE_HOME`, concurrently.
- **Expected:** either per-run files, or a lock so concurrent appends don't interleave/lose records.
- **Actual:** both append to one `provenance.jsonl` with no file lock. In the test the JSONL stayed
  *syntactically* valid (line-buffered appends survived), but there is no guarantee under load —
  interleaved partial writes would corrupt it, and records from different runs are indistinguishable
  (compounds Bug #4).
- **Severity:** **Medium→High** (data integrity under concurrency).
- **Root cause:** append-only file with no `flock`, no run_id.
- **Fix:** `flock` on append + a `run_id` field per record; `trace` groups by run_id.
- **Area:** Data / concurrency.
- **Evidence:** two 200-eval runs → 16 total records in one file; single run → 8.

---

## Bug #13: `error reading <file>` for a missing file is terse + inconsistent exit

- **ICP:** Non-technical founder (ICP1) — typo'd filename.
- **Journey:** first run.
- **Repro:** `axon run /tmp/typo.ax`
- **Expected:** `error: file not found: /tmp/typo.ax` + non-zero exit + maybe "did you mean <nearby>?".
- **Actual:** `error reading /tmp/does_not_exist.ax: No such file or directory (os error 2)` — leaks the
  raw OS error string; exit code was 1 in the audit (inconsistent with the panic-0 paths).
- **Severity:** **Low→Medium** (polish + exit-code consistency).
- **Root cause:** raw `io::Error` surfaced to user.
- **Fix:** friendly message; uniform non-zero exit for all "cannot start" errors.
- **Area:** CLI.
- **Evidence:** the message above.

---

## Bug #14: Type-mismatch error reads expected/found backwards on annotated `let`

- **ICP:** Technical founder (ICP2) after a rapid edit.
- **Journey:** code generation → iteration.
- **Repro:**
  ```
  fn main()->i64{ let x:i64 = "string"  x }
  ```
- **Expected:** "annotation says `i64`, but value is `str`" — i.e. expected i64, found str.
- **Actual:** `[E0102] type mismatch in let type annotation (expected str), found i64` — the
  expected/found are inverted relative to the user's mental model (they wrote `: i64`, gave a str). Then
  E0307 *also* fires redundantly with "(expected i64), found str (expected i64), found str" — the
  expected/found pair is printed twice.
- **Severity:** **Medium** (error quality — wrong direction wastes debugging time; PRD §"Error Messages
  — Structured for AI" means the AI consumer also gets misled).
- **Root cause:** constraint origin swaps annotation-vs-value sides; E0307 message concatenates the
  pair twice.
- **Fix:** normalize so "expected" = the annotation/declared type, "found" = the value's type;
  de-duplicate the E0307 format string.
- **Area:** Backend / inference / error formatting.
- **Evidence:** both error lines above.

---

## Bug #15: Errors print to STDOUT as JSON in `run`, STDERR elsewhere — inconsistent stream discipline

- **ICP:** Power user (ICP5) piping output; CI.
- **Journey:** scripting / integration.
- **Repro:** `axon run /tmp/contra.ax 2>/dev/null` still shows the `{"error": ...}` lines (they're on
  stdout); but `check` and CLI-arg errors go to stderr.
- **Expected:** diagnostics on stderr, program output on stdout — universally. So `axon run x.ax | jq`
  on the program's *own* output isn't polluted by compiler error JSON.
- **Actual:** type-check errors in `run` go to **stdout** as JSON; `clap`/IO errors go to stderr.
  Mixed. A pipeline consuming program stdout gets compiler errors mixed in.
- **Severity:** **Medium** (integration correctness).
- **Root cause:** the `run` path prints diagnostics via stdout `println!` JSON, not stderr.
- **Fix:** all diagnostics → stderr; keep program `println` output → stdout.
- **Area:** CLI / stream discipline.
- **Evidence:** `2>/dev/null` still showed the `{"error":...}` lines.

---

## Bugs #16–30: Catalogued, lower individual severity (grouped)

| # | Title | ICP | Sev | Note |
|---|---|---|---|---|
| 16 | No-args `axon` prints help to **stdout** with exit 2 | ICP1 | Low | Help-on-no-command should arguably be stderr + exit 2 *consistently*; here stdout. |
| 17 | `goal --iterate` 7-day/loop semantics undocumented in `--help` | ICP3 | Low | Discoverability of the iterate flag. |
| 18 | `trace` shows `[improving]` even when last < first on noisy runs | ICP3 | Med | Trend label derived from first→last only, ignores trajectory; can mislabel. |
| 19 | `goal_run(name,...)` with an undefined fn name silently returns `target` | ICP2 | Med | Typo in the fn name → no error, returns the target as if achieved. **Misleading success.** |
| 20 | `dict_to_str` rejects values with `=`/newline by **panic**, not Result | ICP5 | Med | Persistence of arbitrary dict → host panic instead of recoverable error. |
| 21 | `arr_std_f64` on len<2 panics rather than returning Option/Result | ICP5 | Low | Documented, but a panic for a common edge (single sample) is harsh. |
| 22 | `parse_int` on `"  42  "` — trims, but `parse_int("0x1F")` silently errs with generic "parse error" | ICP2 | Low | No radix support surfaced; error not specific. |
| 23 | Empty program / no-main → "panic: no main" exit 0 | ICP1 | High | Same exit-0 family as #1; a brand-new file run gives a *panic*, scary for onboarding. Should be a clean `error:` + non-zero. |
| 24 | `fmt` has no `--check` mode for CI (can't assert "is formatted") | ICP5 | Med | CI can't gate on formatting without diffing manually. |
| 25 | `@[verify]` failure message exposes internal `assert_deployable` fn name to end users | ICP1 | Low | Leaks impl detail in a founder-facing message. |
| 26 | Goal deploy-gate panic is indistinguishable (exit-wise) from a crash | ICP1 | High | Conflated with #2; a *policy* rejection and a *bug* crash need different codes. |
| 27 | `random_i64(hi, lo)` with hi<lo returns `lo` silently (no error) | ICP5 | Low | Inverted args → silent degenerate, not an error. |
| 28 | Recursion-limit panic (6000) is fixed, not configurable; deep-but-valid recursion fails | ICP2 | Med | Legitimate deep recursion (e.g. parsing) hits the guard; no `--max-depth`. |
| 29 | `to_str` vs `to_str_f64` vs `to_str_bool` — three names; wrong one → type error, not auto-dispatch | ICP1 | Med | Onboarding friction; PRD implies inference should pick. A polymorphic `to_str` would remove a whole error class. |
| 30 | No `axon version` / `--version` command | ICP1/ICP5 | Low | Can't tell which build you're on; blocks reproducibility + bug reports. |

---

## Severity rollup (CORRECTED after re-verification)

**CONFIRMED real (exit codes verified clean, no pipe artifact):**
- **High:** #4 provenance merges programs by name · #5 `goal_run` global-not-call best · #6 overflow
  silent wrap (exit 0) · #8 `@[contained]` syntax undiscoverable · #11 RNG non-deterministic / no seed ·
  #19 undefined goal fn name returns `target` silently (exit 0).
- **Medium:** #3 goal validation reports one missing section at a time · #7 doubled "parse error:" prefix ·
  #10 missing-module error doesn't hint AXON_PATH · #12 unsynchronized provenance append under concurrency ·
  #14 backwards/duplicated type-mismatch message · #15 diagnostics on stdout in `run` · #18 `[improving]`
  trend label · #20 `dict_to_str` panics instead of Result · #28 fixed recursion limit · #29 `to_str`
  family not polymorphic.
- **Low:** #13 terse missing-file msg · #16 help-stream · #21 `arr_std_f64` len<2 panic · #22 no radix in
  parse_int · #24 no `fmt --check` · #25 leaks `assert_deployable` name · #27 inverted `random_i64` args ·
  #30 no `axon --version`.

**RETRACTED / downgraded (tester artifact — exit codes are actually correct):**
- ~~#1 "panics exit 0"~~ → **FALSE.** `run` exits 101 on all panics. *Other* observation (uniformity) holds.
- ~~#2 "deploy-gate exits 0"~~ → **FALSE.** `goal` exits 101 on gate failure.
- ~~#23 "no-main exit 0"~~ → **FALSE.** Exits 101. (Sub-point stands: a *panic* for a missing `main` is
  poor onboarding UX — should be a clean `error:` not a `panic:`. Severity Low.)
- ~~#26 "policy-vs-crash conflation"~~ → partially retracted; both exit 101, so they *are* indistinguishable
  exit-wise — but the original "exits 0" framing was wrong. Re-scoped to: deploy-gate rejection deserves a
  *distinct* code (e.g. 3) from a bug-crash (101). Severity Medium, valid.

**Highest-leverage fix order:** #6 + #19 (silent-wrong-value — corrupts the success signal) → #4 + #5
(analytics integrity) → #11 (reproducibility) → #8 + #9 (security feature adoption) → error-message cluster.

---

## ✅ FIXED (2026-05-31, same session)

The top-of-queue confirmed defects are resolved and regression-tested:

| Bug | Fix | Commit | Regression test |
|---|---|---|---|
| #6 overflow silent wrap | `+`/`-`/`*` now `checked_*`, graceful panic on overflow (exit 101) | `3dc9cd6` | `integer_overflow_panics_not_silently_wraps` + 2 |
| #19 typo'd goal fn → target | `goal_run*` errors on unknown name (not defined + no provenance) | `0238f71` | `goal_run_typod_name_errors_not_silent_success` + 2 unit |
| #11 RNG non-deterministic | `srand(n)` builtin + `AXON_SEED` env var; xorshift64 seeded reproducibly | `6c8fe41` | `rng_is_reproducible_under_seed`, `axon_seed_env_var_makes_runs_reproducible` |
| #4 provenance blends programs | `src` field per record; `trace` groups by `(fn, source)`; backward-compatible | `c29e0b1` | `trace_separates_same_named_metrics_from_different_programs` |
| #5 goal_run global-not-call best | Documented (by-design retrospective lookup); doc points at `goal_clear` for isolation | `c29e0b1` | (doc; covered by existing goal tests) |
| #8 @[contained] errors don't teach the fix | E1001/E1004 messages now append a `help:` line with the exact clause to add (least-privilege dir prefix) or remove; grammar added to CLAUDE.md | `18e6108` | `contained_error_messages_suggest_fix` + 3 unit (`dir_prefix_extracts_directory`, enriched `disallowed_write`/`never_read`) |
| #9 no visible capability deny-case | Shipped `examples/asi/contained_violation.ax` (demo #11b) — `axon check` rejects it with E1001; the `all_examples_typecheck_clean` sweep excludes deny-case examples by name | `d694968` | `contained_violation_demo_is_rejected_by_check` |
| #3 goal validation reports one missing section at a time | New `Error::MissingSections(Vec)` collects ALL missing required sections in one error; the validation loop filter-collects instead of returning early | `da54d48` | `missing_sections_lists_all_at_once`, `missing_sections_error_message_names_all`, `goal_with_missing_sections_lists_them_all` |
| #7 doubled "parse error:" prefix | `ParseError::Other` dropped its own `parse error:` prefix — the outer `AxonError::Parse` wrapper adds it. Single prefix now. | `34e0ea5` | `parse_error_prefix_is_not_doubled` |
| #10 missing-module error doesn't hint AXON_PATH | E0901 now appends an AXON_PATH hint when the env var is unset (the lever every in-repo demo needs); suppressed when it's already set | `fa9eeb5` | `missing_module_error_hints_axon_path_when_unset` |
| #14 backwards/duplicated type-mismatch message | E0102 now maps `constrain(found, expected)` so "expected"=annotation, "found"=value; E0307 message trimmed to `return type mismatch` (structured fields carry the pair, no longer doubled) | `11d13e8` | `let_annotation_mismatch_reports_annotation_as_expected` |
| #15 diagnostics on stdout in `run` | Verified already-fixed: `cmd_run` emits every diagnostic via `emit_error`→`eprintln!` (stderr); program output stays on stdout. No code change. | _(verified)_ | covered by `2>/dev/null` repro |
| #18 `trace` `[improving]` on degrading runs | Trend now from least-squares slope over the whole series (scale-invariant dead-zone), not first-vs-last; a peaked-then-collapsed run reads `regressing` | `c5fd613` | `peaked_then_collapsed_is_not_improving` + 7 (`trend_label` suite) |
| #20 `dict_to_str` panics on `=`/newline | Now returns `Result<str,str>`: un-representable key/value yields `Err(msg)`, not a host panic (`no exceptions — Result everywhere`). Sole `.ax` caller (`persistent_bandit`) and the round-trip integration test updated to match `Ok`. | `5eaa8fc` | `dict_to_str_bad_key_returns_err_not_panic`, `dict_to_str_newline_value_returns_err_not_panic`, `dict_to_str_clean_dict_round_trips` |
| #28 fixed recursion limit | `AXON_MAX_DEPTH` overrides the default 6000 ceiling (clamped to 1,000,000). The interpreter thread stack scales with the limit (`stack_size_for_depth`), preserving the invariant that the graceful guard fires *before* a real stack overflow — verified: infinite recursion at limit=500000 still exits 101, not SIGABRT. Panic message now teaches the lever. | `3c19d87` | `axon_max_depth_raises_recursion_limit` + 4 unit (`max_depth_*`, `stack_grows_with_depth_but_never_below_floor`) |
| #29 `to_str` not polymorphic | `to_str` now accepts any scalar (i64/i32/.../f64/bool), dispatching on the runtime value — picking `to_str_f64`/`to_str_bool` is no longer required (they remain valid). Special-cased in infer + checker (mirrors the `len` precedent), interp dispatches via `display()`. Non-scalars (`str`, `[T]`, `Option`) still error, so type safety holds. New `Type::is_scalar()`. | `e99e1a9` | `to_str_is_polymorphic_over_scalars`, `to_str_polymorphic_matches_specialized_in_interpolation`, `scalar_classification` |
| #26 deploy-gate code not distinct from crash | New `Flow::VerifyFailed` variant → exit **3** for `@[verify]`/deploy-gate rejections; genuine panics stay **101**. Native runtime (`__axon_verify_panic`) aligned: `abort()` → `exit(3)`, so interp↔codegen agree. CI can now branch policy-rejection vs bug-crash. Documented in I-8. | `550f2f8` | `verify_failure_and_crash_have_distinct_exit_codes`; updated `verify_gate_panics_when_underconfident`, `goal_demos_pure_outcomes` |
| #27 `random_i64` inverted bounds silent | `hi < lo` now panics with a fix-teaching message (inverted args are a caller error, not a silent `lo` — I-9). `hi == lo` returns `lo` (empty range, documented boundary). | `02cd617` | `random_i64_inverted_bounds_panics_not_silent`, `random_i64_empty_range_returns_lo`, `random_i64_valid_bounds_stay_in_range`, `random_i64_inverted_bounds_is_panic` |
| #30 no `--version` / build identity | Partially stale: `--version`/`-V` already worked via clap (`axon 0.1.0`), and an unrecognized subcommand already exits 2 (the original "exit 0" was a `\| head` pipe artifact — same trap this hunt documented). The *substantive* fix shipped: a `build.rs` captures the git short SHA + dirty flag into `AXON_GIT_SHA`, so `--version` now reports `axon 0.1.0 (02cd617)` — a reproducible build identity a bug report can pin. Falls back to `(unknown)` without git. | `7cebbd1` | `version_reports_build_identity` |
| #25 `@[verify]` leaks `assert_deployable` symbol | Deploy-gate failures read `verify failed in the deploy gate` instead of leaking the generated `assert_deployable` symbol to non-technical founders. Author-named verify fns keep their own names. Fixed in BOTH paths: interp (`verify_fn_label`) and native runtime (axon-rt `__axon_verify_panic`), kept in sync. | `64ee687` | `verify_label_hides_generated_deploy_gate_name`, `verify_label_keeps_author_named_functions` (core); `message_hides_generated_deploy_gate_symbol` (rt) |
| #22 `parse_int` generic "parse error" | `parse_int`/`parse_float`/`parse_bool` `Err` messages now name the offending input (`could not parse \`0x1F\` as a base-10 integer`); a radix prefix (`0x`/`0o`/`0b`) adds a strip-the-prefix hint. Whitespace trimming + valid parsing unchanged. | _(this commit)_ | `parse_int_error_message_is_specific` (CLI); `parse_int_radix_prefix_errs_with_hint`, `parse_int_still_trims_and_parses_decimal` (unit) |

**Remaining open** (next queue, not yet done): Low-severity polish (#16 help-stream, #17 iterate-flag
docs, #21 `arr_std_f64` len<2, #24 `fmt --check`) plus this session's logged findings (#31–37 —
several are codegen-parity items gated on the slow build). **All Medium+ confirmed bugs from the
original hunt are resolved**; #22, #25, #27, #30 closed. None of the remainder are
success-signal-critical. **The original bug-hunt backlog is effectively cleared** — next work should
shift to unfinished PRD/ROADMAP capabilities (see below).

### More findings (logged this session, cont.)

| # | Title | Sev | Area | Note |
|---|---|---|---|---|
| 36 | codegen `random_i64` UB/div-by-zero on degenerate bounds | Med | Codegen | After #27 the interpreter rejects `hi < lo` and handles `hi == lo`; the codegen IR does neither — `hi == lo` is a signed-rem div-by-zero (hard crash), `hi < lo` is garbage. Needs a matching IR guard. Deferred: codegen build is pathologically slow (BUILD_DIAGNOSIS.md). Commented at the emit site. Interp is reference. |
| 37 | codegen `parse_int` accepts trailing garbage + empty Err message | Med | Codegen | The IR uses `strtoll(base 10)`, which stops at the first non-digit and reports success if it consumed any leading digit — so `parse_int("0x1F")` returns `Ok(0)` in codegen but `Err` in the interpreter, and the codegen Err payload is an empty string (no message). Needs an endptr-reaches-end check + a real message. Deferred (slow codegen build); commented at the emit site. Interp is reference. |
| 38 | codegen `str_reverse` byte-reverses (mangles UTF-8) | Med | Codegen | Codegen's inline `str_reverse` reverses BYTES (`buf[i]=s[len-1-i]`); the interpreter reverses chars (`chars().rev()`). On multibyte input they disagree and codegen produces invalid UTF-8 (`str_reverse("héllo")` → garbage vs interp's `olléh`). **Resolved by R1 migration:** the axon-rt port matches the interpreter (I-2 canonical) and codegen's byte-reverse body is deleted. ASCII-only fixtures never exposed it. See `governance/specs/R1b-str-return-abi.md` §12 Q2. |
| 39 | codegen `str_replace` skips empty `from` | Low | Codegen | Codegen's inline `str_replace` skips replacement when `from==""` (copies `s`); the interpreter uses Rust `str::replace`, which interleaves `to` (`str_replace("abc","","X")` → `XaXbXcX`). **Resolved by R1 migration:** the axon-rt port matches the interpreter (I-2 canonical); codegen's skip-guard body is deleted. Existing fixtures use non-empty `from` only, so untested before now. See `R1b-str-return-abi.md` §12 Q3. |
| 40 | codegen `to_str(f64\|bool)` SILENTLY truncates — polymorphic-to_str parity gap | **Med** | Codegen / R1 | **Re-diagnosed (2026-06-01):** NOT an IR-verify crash (that's a #41 cascade). `axon build` of `to_str(3.5)` builds but prints `3` (float truncated to int); `to_str(true)` prints `-1`. Codegen's `to_str` is declared `i64→str`; for f64/bool the generic arg-coercion (float→int) silently mangles the value. The interpreter (oracle) made `to_str` polymorphic (#29); codegen must dispatch `to_str(x)` per arg type (i64→to_str, f64→to_str_f64, bool→to_str_bool) at the call site, like the string-interpolation path (`expr.rs:728` `emit_fmt`) ALREADY does correctly. The direct-call path doesn't. Silent-wrong-value (I-9 family). |
| 41 | codegen closure-passing ABI fails IR verification — ROOT CAUSE of the native-build crashes | **High** | Codegen / R1 | **Re-diagnosed (2026-06-01):** the actual root cause. A minimal program passing a lambda to a higher-order fn (`apply(g, 21)`) fails IR verification — `Call parameter type does not match function signature`, closure passed as `{ptr, ptr}` (fn-ptr + env-ptr) whose type ≠ the callee's declared param. The `to_str()` 0-arg errors in fibonacci.ax are a CASCADE from this malformed IR, not independent. Also feature_tour: enum `==` routed to `str_eq(%Op_enum, %Op_enum)` instead of an enum-tag-compare path. Native closure-passing ABI + enum-equality codegen incomplete. **Fix FIRST — unblocks 3 of 4 failing examples.** Interp handles both. **FIXED** 29fea3b (closures) / 0e61bd2 (enum ==). |
| 42 | codegen array-of-struct under-allocates → heap corruption | **High** | Codegen / R1 | `axon build` of an array containing enum/struct VALUES (`[Shape::Circle{..}, Shape::Rect{..}]`) crashes at runtime: `malloc(): corrupted top size`, SIGABRT. Root cause (emit_array_lit, expr.rs ~1488): element size was HARDCODED to 8 bytes for ANY struct/array/ptr type, but an enum is `{i32 tag, [N x i8]}` (16-24B), so the array mallocs `8×n` while the per-element GEP-store writes the full struct → writes past the buffer → heap corruption. Memory-unsafe. **FIXED** (this commit): use `elem_ty.size_of()` (LLVM's exact ABI size) for the malloc, matching the GEP stride. Verified: array-of-enum + traits_demo native==interp; int/float/slice arrays unregressed. (traits_demo is enum ADTs, NOT Phase-3 traits — re-diagnosed.) |

### More findings (logged this session, cont.)

| # | Title | Sev | Area | Note |
|---|---|---|---|---|
| 34 | Redteam-gate rejection exits 1, not the policy code 3 | Low | Surface / goal | After #26, `@[verify]` rejections exit 3, but a `redteam_check()` failure (redteam-goal.md) blocks deploy via `exit(1)`. For pipeline consistency, *all* policy rejections (verify, redteam, future gates) arguably deserve the same class code (3). Distinct path today; deferred — unifying goal-gate exit codes is its own small spec item. |
| 35 | `axon-rt` has 22 clippy `not_unsafe_ptr_arg_deref` errors | Low | Runtime / lint hygiene | The FFI `extern "C"` functions in `goal.rs`/`adaptive_registry.rs`/`lib.rs` deref raw pointers without being marked `unsafe fn`. Pre-existing; invisible to CI because the clippy gate is scoped to `-p axon-core` only. Fix: mark the FFI fns `unsafe` (or `#[allow]` with rationale) and extend the CI clippy gate to `--workspace`. |

### More findings (logged this session)

| # | Title | Sev | Area | Note |
|---|---|---|---|---|
| 33 | `to_str` interp↔codegen parity gap after #29 | Med | Codegen | Interp made `to_str` polymorphic over scalars; codegen still declares only the i64 form, and the generic float→int arg coercion would truncate `to_str(3.14)` → "3" instead of "3.14". Needs per-arg-type dispatch in `codegen/builtins.rs`. Deferred: codegen build is pathologically slow (BUILD_DIAGNOSIS.md), can't verify in-loop. Commented at the declaration site. |

### New findings (logged this session)

| # | Title | Sev | Area | Note |
|---|---|---|---|---|
| 31 | `dict_from_str` panics on a malformed line (no `=`) | Low | Interp / robustness | Same family as #20 (host panic on recoverable input), sibling function. Should return `Result<Dict,str>` for symmetry. Deferred to keep #20's tick atomic. |
| 32 | Test code trips `clippy::approx_constant` under `--all-targets` | Low | Test hygiene | `infer.rs:1621` and `verify.rs:777` use `3.14` as a sample float; `cargo clippy --all-targets` flags it as approximate PI. CI only gates lib+bin (`-p axon-core -- -D warnings`), so it's invisible there. Pre-existing (commits `76f8b75`, `390c692d`). Fix: use a non-PI sample (e.g. `2.5`) or `#[allow]`. |

**The through-line (corrected):** exit codes turned out *honest* (good news — I was wrong in the first
pass and verified it). The real erosion of the success signal is **plausible-looking wrong values on
degenerate input** (#6 overflow, #19 typo'd fn name, #27 inverted args): the program exits 0 *and* prints
a number, so neither a human nor an autonomous loop sees anything wrong — the worst kind of bug, because
the green check lies. Fix that family first. **The QA-process lesson is itself a finding:** my own first
pass shipped a false Critical because a shell pipe masked the exit code — proof that even adversarial
testing needs its results *verified clean*, which is now codified in `TESTING_STANDARD.md` Layer 6
("verify the reproduction without the harness in the way before logging a Critical").

---

## Should this kind of testing be in `TESTING_STANDARD.md`?

**Yes — emphatically.** See the new **Layer 6: Journey / Red-Team testing** added to the standard.
The five layers there test *units and seams*; none of them would have caught a single bug in this
report, because every bug here lives in the **integration between the CLI, the user's intent, and the
exit signal** — exactly the space unit tests don't see. An autonomous ASI grading its own green tests
would have shipped all 30 of these.
