# Axon Requirements — Top-10 Traceability Matrix

**Source of truth:** `/home/cklaus/projects/BTask/packages/bcode/AI_Language_Plan.md` (the PRD).
**This file:** maps the PRD's ~50 features down to the 10 load-bearing requirements,
records honest current completion, names the gap, and points at the spec + acceptance
tests that close it.

> **For the autonomous builder:** Before starting *any* feature, find its requirement
> here. If the requirement has no spec, write the spec first (`SPEC_TEMPLATE.md`). If it
> has no acceptance test, the feature is not done until that test exists and passes.
> Update the **Status** and **%** columns in the same commit that changes the truth.

---

## Ranking method

Requirements are ranked by **load-bearing-ness**: how many other requirements collapse
if this one is absent or wrong. R1, R2, R3, R10 are foundational; the rest layer on top.

A requirement is **DONE** only when its acceptance tests pass *and* it holds under
`DEFINITION_OF_DONE.md`. "Partial" means real code exists but the acceptance set is
incomplete.

---

## The matrix

| # | Requirement | PRD § | % | Status | Gap (what's missing) | Spec | Acceptance anchor |
|---|---|---|---|---|---|---|---|
| **R1** | Native compiler pipeline at Tier-1 perf (parse→typecheck→borrow→LLVM→native) | Compiler Architecture; Build Phases | 90 | ✅ Working | **Build SOLVED + codegen green.** Stall was a serde×codegen default collision (`BUILD_RESOLVED.md`); fixed by dropping serde-json from default (`cargo build -p axon-core` = ~3s native compiler). Native==interp on **25/28 fn-main examples**; all real codegen crashes/correctness bugs fixed: #40 to_str dispatch, #41 closure-passing ABI + enum==, #42 array-of-struct heap corruption (size_of), tuples (literals/access/destructure/nested/arrays). 3 residuals build clean and are NOT codegen bugs (2 AI-call env; 1 = #37 parse_int Err-message gap). **#37 FIXED** (codegen parse_int now rejects trailing garbage + carries a real Err message — native==interp on mixed parse_int inputs, verified via axon build). **Perf benchmark DONE** (`scripts/perf_bench.sh`): native AOT is **6.0x faster** than the interpreter across 9 pure-compute programs (min-of-N wall-clock; debug-opt, release would widen it). Tier-1 perf evidenced. (Logged BUG_HUNT #43: `axon build --release` has a duplicate-std-symbol link error — debug build works; the bench uses it.) | `spec/compiler-phase1.md`; `BUILD_RESOLVED.md`; `BUILD_DIAGNOSIS{,_2,_3}.md` | A native binary of `examples/*.ax` runs and matches interpreter output ✅ (25/28) + a perf-tier benchmark (pending) |
| **R2** | Type system + borrow checker (HM inference, no null, Result/Option, 2-mode ownership) | Type System | 90 | ✅ Strong | Edge cases in generic + trait resolution; refinement types (Phase 5) not landed | `spec/compiler-phase{2,3}.md`; `spec/compiler-phase5.md` (draft) | `cargo test` infer/checker/borrow suites + `all_examples_typecheck_clean` |
| **R3** | AI as language primitive — `std.ai`, model routing, `#[ai(policy)]`, deterministic fallback | AI as Primitive; AI Policy | 75 | ⚠️ Partial | **AiCall provenance record DONE** (§4.3): every `ai_complete` appends one `event:"ai_call"` NDJSON record (tier/model/params_hash/**prompt_hash SHA-256, not verbatim**/mode/reason/cost_usd), deterministic (R9 replay key). **OFFLINE FALLBACK + E1300 DONE** (§3.3/§6): `@[ai(policy(fallback: "x"))]` parses (nested-group attr form + flat form) and, when offline, returns `Ok("x")` stamped `mode:"fallback"` — the program stays TOTAL; no fallback offline = coded **E1300** (not a generic panic). **TIER ROUTING (policy+default) DONE**: `ai_routing.rs` (cheap/balanced/strong → concrete model+version host table); `@[ai(policy(tier: X))]` resolves the tier (default balanced), and the AiCall provenance now records the *real* routed `(tier, model, version)` instead of a hardcoded "balanced"/"mock" placeholder. Unknown tier → E1302; AI call with no `@[ai(policy)]` → W1310 (un-metered warning). **PER-CALL TIER DONE** (§R3b): `f(args, tier: cheap)` — a scoped additive `Expr::Call.tier` field (NOT a general named-arg grammar); resolution is per-call > policy > default; unknown per-call tier → E1302; codegen ignores it (interp-side AI routing). `axon ai policy` CLI **DONE** (prints resolved tier/model/fallback per @[ai] fn). Remaining: budget gate E1301 (Phase-7 Budget). | 📝 `governance/specs/R3-ai-primitive.md` (Reviewed), `governance/specs/R3b-named-tier-arg.md` | Policy-gated fallback fires offline ✅ (E1300); **tier routing pins real model ✅** (policy+default; E1302/W1310); **per-call `tier:` override ✅** (per-call > policy > default); **ai_call provenance recorded ✅** |
| **R4** | Three code zones: Static / Adaptive / Agent + compiler-enforced provenance | Three Code Zones; Error/Perf Logging | 88 | ⚠️ Partial | `@[adaptive]`/`@[experiment]`/`@[agent]` all behaviorally distinct now. `@[experiment]` injects tagged provenance (`zone:"experiment"`+label) but is excluded from `goal_run` best. **`@[agent]` MANDATORY ACTION-LOG DONE**: `call_builtin` injects an `event:"agent_action"` record (`zone:agent`, `action`=tool, `caps_used`=capability) whenever a capability-bearing builtin runs inside an `@[agent]` fn — compiler-injected, un-opt-out-able (I-13); non-agent fns doing the same call log nothing. JSONL carries real `event`/`zone`/`label`. **CODEGEN PROVENANCE TRIPWIRE DONE** (now R1 native builds): `scripts/provenance_parity.sh` + `codegen_provenance_matches_interp_on_adaptive_returns` compile one `@[adaptive]` program both ways and assert the native binary's return records carry the SAME discriminating fields (`event:"adaptive_return"`/`zone:"adaptive"`/`fn`/`score`) the interpreter writes. **Found+fixed a real I-13 codegen hole**: native `__axon_provenance_log_ret_{i64,f64}` was writing a degraded `event:"event"` record with no `zone` — fixed in `axon-rt/provenance.rs`. Residual: native records omit `input`/`src` (ABI enrichment), agent-action codegen path (Phase-7 tools). | `ROADMAP.md` §6–7; ✅ `governance/specs/R4-code-zones.md` (Reviewed — interp slice + codegen parity tripwire landed) | Adaptive fn cannot compile without provenance injection ✅ (zone injection engine-keyed, no opt-out); `@[experiment]` distinct ✅ (cli_run: distinct-from-adaptive + trace-survives); agent action log mandatory ✅ (cli_run: agent_actions_are_mandatorily_logged); **codegen↔interp provenance parity ✅** (provenance_parity.sh; native matches interp record shape) |
| **R5** | Goal-directed optimization — `#[goal]` type, 3 strategies, deterministic test sets, eval hierarchy | Autonomous Optimization | 82 | ⚠️ Partial | 4 runtime strategies shipped (`goal_run`/`_continue`/`_random`/`_multistart`); **eval hierarchy STARTED**: `goal_eval(name, input)` does HELD-OUT evaluation — runs the metric on a held-out point WITHOUT recording it as a training probe (snapshot+restore, no overfitting/no provenance pollution), the train→holdout→target-gate primitive. **`#[goal(metric, target, max_evals, holdout)]` ATTRIBUTE SUGAR DONE**: auto-wires goal_run (train) + goal_eval (held-out) + a gate, injecting `goal_met` into the fn body; E1500/E1503/E1504 validation. Still missing: first-class `Goal` *value type* (passable/composable — Phase-7); list-literal `test_set: [...]` (attr grammar is flat key:value). | `ROADMAP.md` §5; `spec/stdlib.md` | `#[goal(metric, test_set, target)]` runs to target on a held-out set ✅ (via goal_run+goal_eval); provenance per experiment ✅; `#[goal]` attr sugar (pending) |
| **R6** | Capability security — content-addressed imports, compile-time I/O restriction, AI audit on import | Supply Chain Security; Capability Permissions | 82 | ⚠️ Partial | `@[contained]` enforces net/fs/exec at fn boundary. **Import-edge boundary VERIFIED (I-11)**: static cap checker catches a `mod`+`use`-imported fn's `@[contained]`/`never:` violation (E1004) on the merged program. **Content-addressed lockfile WORKING end-to-end**: `lockfile.rs` (zero-dep `axh1:` SHA-256 + deterministic `axon.lock`) wired to CLI — `axon lock <file>` hashes each `use`d module + writes the lock; `axon verify-lock <file>` recomputes and rejects a tampered module with **E1201** (exit 2), a missing entry with **E1202**. The PRD's "tampered content hash rejected" is delivered. **Import-edge cap-widening DONE (E1203)**: a `@[contained]` importer that imports a module exercising a capability it doesn't grant is rejected at `axon check` (`check_import_capabilities`, opt-in: uncontained importers unaffected — back-compat). **Transitive-`use` hashing DONE**: `axon lock`/`verify-lock`/E1203 now cover the whole import closure (prog→mid→leaf all pinned; a tampered deep module is E1201). **AUDIT-ON-IMPORT DONE (E1204)**: `audit.rs` — `axon lock` audits each module's capability surface (`audit_module`) and pins a deterministic verdict (`clear`/`flagged`/`denied`) into `axon.lock` as `<verdict>:<hash>`; on every build the verdict is re-validated by hash (**no AI call at compile time**); a `denied` verdict (undeclared `net` — the exfiltration channel) blocks the build with E1204. Honest deviation: spec names `exec` as denied tier but Axon has no exec builtin yet, so `denied` keys on undeclared `net` (the detectable channel); AI-audit *quality* layer (obfuscation reasoning via R3) is the documented follow-slice. | 📝 `governance/specs/R6-capability-security.md` (Reviewed) | Import without declared capability fails compile ✅ (E1004 + **E1203 import-edge**); **tampered content hash rejected ✅** (axon lock/verify-lock → E1201); **audit on import ✅** (E1204; deterministic verdict pinned + hash-revalidated) |
| **R7** | Cross-platform targets — native / wasm / js / mobile from one source | Cross-Platform; Mobile; UI; 3D | 50 | ⚠️ Partial | **Slice A DONE — Axon runs on wasm.** The tree-walking interpreter compiles to `wasm32-wasip1` and runs `.ax` headless under `wasmtime` with **identical exit codes + stdout to native on 15/15 pure-compute examples** (same `interp.rs`, two targets — identical by construction, I-2). One host touchpoint changed: `on_deep_stack` runs on the single wasm stack (`#[cfg(wasm32)]`); 64 MiB wasm stack (`.cargo/config.toml`) + `RECURSION_LIMIT=450` so deep recursion fails with the same graceful panic as native, no trap. **`axon target` CLI + AxonHost SEAM DONE**: the 5 host touchpoints (read_file/write_file/env_var/now_ms/sleep_ms) route through a thread-local `AxonHost` (DefaultHost==std on native, byte-identical; a browser host plugs in later). Remaining: a concrete BrowserHost (wasm-bindgen glue), js/mobile, AOT-wasm Slice B. | 📝 `governance/specs/R7-targets.md` (Reviewed — Slice A landed) | Same `.ax` runs on native and wasm with identical observable results ✅ (15/15 pure-compute, `scripts/wasm_parity.sh`) |
| **R8** | Built-in testing + structured errors — `#[test]`, property-based `forall`, AI-parseable diagnostics | Testing; Error Messages | 96 | ⚠️ Partial | `@[test]`/`should_fail` + **`forall` property testing DONE**: `@[test] @[forall(n: N)]` randomizes typed params (i64/f64/bool/str) over N cases via seeded RNG, and on failure BINARY-SEARCH-SHRINKS to the minimal counterexample (`a < 50` → `a=50`) with a reproduce seed. **Versioned machine-stable diagnostic schema DONE** (`axon-diag/1`, `diag_schema.rs`): `axon check --json` emits structured `{schema,severity,code,message,help}` NDJSON (code as a first-class field, version-tagged). **TYPED END-TO-END DIAGNOSTIC DONE** (§10): `run_check_pipeline_located` resolves each typed error's byte-`Span` → (line,col) via `SourceMap` and returns located `PipelineDiagnostic`s; `PipelineDiagnostic::json()` emits `axon-diag/1` WITH `file`/`line`/`col` first-class; `axon check --json` now routes through it (E0102 mismatch → `"line":3,"col":5`). Residual: standalone parse-error emit path is span-less; `expected`/`found`/`help` not yet split into discrete typed fields (axon-diag/2). | `TESTING_STANDARD.md`; `spec/stdlib.md`; `governance/specs/R8-diagnostic-schema.md` | `forall(a,b){assert(f(a,b)==f(b,a))}` runs N cases ✅ + shrinks ✅; error JSON schema versioned ✅; **diagnostics carry source location end-to-end ✅** (check_json_includes_source_location) |
| **R9** | Layer-1/3 alignment — `Uncertain<T>`, `Temporal<T>`, `#[contained]`+`#[corrigible]`, `#[verify]` | ASI Extensions; Structural Alignment; Formal Verification | 72 | ⚠️ Partial | Uncertain/Temporal/`@[verify]`(runtime, composite preds)/`@[contained]` done; **`@[corrigible]` LATCHING KILL-SWITCH DONE**: `corrigible_halt()` trips a one-way latch (no resume builtin by design); once latched, every `@[corrigible]` fn call is refused before its body runs (engine-enforced, keyed on the annotation) — `corrigible_halted()` lets a loop wind down gracefully, an unguarded post-halt call fails closed with a distinct exit code 4 (`Flow::Halted`, distinct from panic 101 / verify 3 / static 2). The latch is targeted (non-corrigible fns still run). **SMT-backed `@[verify]` DONE** (`smt` feature, opt-in): `axon verify` encodes a fn's body as a Z3 term and PROVES the `value OP K` bound for all inputs (unsat negation) or reports a concrete counterexample (E1102, e.g. `dec(x)=x-1` violated at x=0); out-of-fragment (loops/calls/float) → W1103 (runtime gate applies). Straight-line integer fragment; loops/recursion-invariants + float theory remain. Z3 isolated behind the feature — default build never links it. | `spec/compiler-phase5.md` (SMT); `ROADMAP.md` §0,§7 | `@[corrigible]` kill-switch latches ✅ (cli_run: latches+freezes / fail-closed exit 4 / targeted scope); `#[verify]` refinement proven by Z3 on a sample (pending) |
| **R10** | Self-improving compiler — learned optimization passes graduated from AI-discovered asm | Self-Improving Compiler; Intelligence Maturity | 85 | ⚠️ Partial | **Verification harness + graduation firewall BUILT.** Four gates (`improve.rs`): G1 correctness (interpreter oracle, whole corpus, E1401; never AI — E1406 by construction), G2 capability-diff (I-12, E1402), G3 regression (`@[test]` outcome preserved, E1403 — catches what G1 misses), G4 perf (opt-in timing, advisory `faster` claim, W1410). Graduation (`manifest.rs` + `axon improve` CLI): `graduate` enforces multi-sig of ≥2 **distinct** root Principals (E1404 — a duplicate signer / self-graduation is refused); content-addressed `passes.manifest` (`axp1:`/`axv1:`/`axc1:`), malformed → E1405; a pass not in the manifest never runs; `list`/`revert` round-trip. Only `discover` (the unprivileged AI proposal side) remains — a proposal grants nothing; only graduation grants execution. | 📝 `governance/specs/R10-self-improving-compiler.md` (Reviewed) | A discovered pattern verified correct (G1 ✅) + safe (G2 ✅) + non-regressing (G3 ✅) + faster (G4 ✅), graduation gated by multi-sig (E1404 ✅); AI discovery of candidates pending |

**Weighted completion:** language-core ≈ 55% · by-PRD-phase ≈ 30% · full-platform vision ≈ 12%.

---

## Reading the gaps as a work queue

Ordered by **(load-bearing × cheapness-to-close)** — the autonomous builder's default priority:

1. **R8 `forall` property testing** — cheap, unblocks deeper testing of *everything else*. Do first.
2. **R5 `#[goal]` first-class** — the autonomy engine is closest to complete; finishing it compounds.
3. **R9 `#[corrigible]`** — small surface, large safety payoff; pairs with existing `@[contained]`.
4. **R3 AI routing + `#[ai(policy)]`** — the differentiation; needs a spec first (forks on reproducibility).
5. **R6 capability/registry** — security gate; spec-first, because under-spec = exfiltration risk.
6. **R1 native build** — high value, high risk; the fix is prototyped but unvalidated. Schedule a focused effort, not a tick.
7. **R7 targets / R10 self-improving** — largest, most speculative; spec now, build after R1 lands.

---

## Update protocol

- Any commit that changes a requirement's truth **must** update its `%`, `Status`, and `Gap`.
- A requirement moving to ✅ DONE **must** cite the passing acceptance test in the commit body.
- New PRD features get a row here *before* implementation — if it's not in the matrix, it's not a tracked requirement.
- The weighted-completion line is recomputed whenever a `%` changes.
