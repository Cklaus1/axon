# Axon Requirements — Top-10 Traceability Matrix

**Source of truth:** `/home/cklaus/projects/BTask/packages/bcode/AI_Language_Plan.md` (the PRD).
**This file:** maps the PRD's ~50 features down to the 10 load-bearing requirements,
records honest current completion, names the gap, and points at the spec + acceptance
tests that close it.

> **For the autonomous builder:** Before starting *any* feature, find its requirement
> here. If the requirement has no spec, write the spec first (`SPEC_TEMPLATE.md`). If it
> has no acceptance test, the feature is not done until that test exists and passes.
> Update the **Status** and **%** columns in the same commit that changes the truth.

---

## Ranking method

Requirements are ranked by **load-bearing-ness**: how many other requirements collapse
if this one is absent or wrong. R1, R2, R3, R10 are foundational; the rest layer on top.

A requirement is **DONE** only when its acceptance tests pass *and* it holds under
`DEFINITION_OF_DONE.md`. "Partial" means real code exists but the acceptance set is
incomplete.

---

## The matrix

| # | Requirement | PRD § | % | Status | Gap (what's missing) | Spec | Acceptance anchor |
|---|---|---|---|---|---|---|---|
| **R1** | Native compiler pipeline at Tier-1 perf (parse→typecheck→borrow→LLVM→native) | Compiler Architecture; Build Phases | 95 | ✅ Working | **Build SOLVED + codegen green.** Stall was a serde×codegen default collision (`BUILD_RESOLVED.md`); fixed by dropping serde-json from default (`cargo build -p axon-core` = ~3s native compiler). Native==interp on **ALL 28/28 fn-main examples under `AXON_AI_MOCK=1`** (`scripts/all_examples_parity.sh` + cli `all_examples_native_match_interp_under_mock`). **Closed the last 2:** the 2 AI examples used to differ ONLY because native codegen ignored `AXON_AI_MOCK` (axon-ai's `__axon_ai_complete` read `ANTHROPIC_API_KEY` and errored), while the interpreter returned a deterministic stub — fixed by making `ai_complete_inner` honor `AXON_AI_MOCK` and return the interpreter's exact stub (`MOCK_AI_COMPLETE`), so native AI output is byte-identical under mock. (Live, un-mocked AI is inherently non-deterministic and not part of byte-parity by design.) all real codegen crashes/correctness bugs fixed: #40 to_str dispatch, #41 closure-passing ABI + enum==, #42 array-of-struct heap corruption (size_of), #36 random_i64 SIGFPE, #38/#39 str_reverse/replace UTF-8, tuples. **#37 parse_int Err message NOW BYTE-IDENTICAL** (codegen delegates to axon-rt's `__axon_parse_int_err`, echoing the input + radix hint like the interp — closed the last non-AI divergence, `error_handling.ax` now identical). **Perf benchmark DONE** (`scripts/perf_bench.sh`): native AOT is **6.0x faster** than the interpreter across 9 pure-compute programs. **`axon build --release` FIXED** (#43, `-Wl,--allow-multiple-definition`). | `spec/compiler-phase1.md`; `BUILD_RESOLVED.md`; `BUILD_DIAGNOSIS{,_2,_3}.md` | A native binary of `examples/*.ax` runs and matches interpreter output ✅ (**28/28 under mock**, scripts/all_examples_parity.sh) + perf-tier benchmark ✅ (scripts/perf_bench.sh, 6.0x faster than interp) |
| **R2** | Type system + borrow checker (HM inference, no null, Result/Option, 2-mode ownership) | Type System | 90 | ✅ Strong | Edge cases in generic + trait resolution; refinement types (Phase 5) not landed | `spec/compiler-phase{2,3}.md`; `spec/compiler-phase5.md` (draft) | `cargo test` infer/checker/borrow suites + `all_examples_typecheck_clean` |
| **R3** | AI as language primitive — `std.ai`, model routing, `#[ai(policy)]`, deterministic fallback | AI as Primitive; AI Policy | 87 | ⚠️ Partial | **AiCall provenance record DONE** (§4.3): every `ai_complete` appends one `event:"ai_call"` NDJSON record (tier/model/params_hash/**prompt_hash SHA-256, not verbatim**/mode/reason/cost_usd), deterministic (R9 replay key). **OFFLINE FALLBACK + E1300 DONE** (§3.3/§6): `@[ai(policy(fallback: "x"))]` parses (nested-group attr form + flat form) and, when offline, returns `Ok("x")` stamped `mode:"fallback"` — the program stays TOTAL; no fallback offline = coded **E1300** (not a generic panic). **TIER ROUTING (policy+default) DONE**: `ai_routing.rs` (cheap/balanced/strong → concrete model+version host table); `@[ai(policy(tier: X))]` resolves the tier (default balanced), and the AiCall provenance now records the *real* routed `(tier, model, version)` instead of a hardcoded "balanced"/"mock" placeholder. Unknown tier → E1302; AI call with no `@[ai(policy)]` → W1310 (un-metered warning). **PER-CALL TIER DONE** (§R3b): `f(args, tier: cheap)` — a scoped additive `Expr::Call.tier` field (NOT a general named-arg grammar); resolution is per-call > policy > default; unknown per-call tier → E1302; codegen ignores it (interp-side AI routing). `axon ai policy` CLI **DONE** (prints resolved tier/model/fallback per @[ai] fn). **BUDGET METER + E1301 DONE** (§R3c, first concrete Phase-7 `Budget`): `@[ai(policy(budget: N))]` meters every `ai_complete` the fn makes and halts the (N+1)th with E1301 before any model dispatch; per-activation (each call to a budgeted fn gets a fresh N via `AiBudgetGuard`); `budget: 0` blocks the first call; no budget → unmetered (back-compat); malformed → W1311 + unmetered. **PER-TOKEN COST METER DONE** (Phase-7 kernel `cost_meter`, closes ROADMAP F4): `ai_routing::Tier::rate_micro` adds a per-tier cost rate (µ$/1k tokens, cheap<balanced<strong); every dispatched `ai_complete` charges `tier.cost_micro(est_tokens)` to a run-global meter, stamps the **real `cost_usd`** into the `ai_call` provenance (was hardcoded 0), and exposes the total via the `ai_cost_spent() -> i64` (µ$) builtin — the kernel counterpart to `llm_gateway.ax`. Deterministic (token estimate from prompt length) ⇒ works under mock. **LIVE-REQUEST TIER ROUTING DONE (was a real bug)**: `axon-ai` previously hardcoded `model:"claude-sonnet-4-6"` in the live request and ignored the resolved tier — so the provenance recorded the right model but the *actual call* always hit sonnet regardless of `strong`/`cheap`. Fixed: `ai_routing::Tier::api_model()` returns the wire model (`claude-haiku-4-5`/`claude-sonnet-4-6`/`claude-opus-4-8`, each **env-overridable** via `AXON_AI_MODEL_{CHEAP,BALANCED,STRONG}` for a proxy/gateway deployment), and the interp's live `ai_complete` now calls `axon_ai::complete_with_model(prompt, tier.api_model())` — so selecting `strong` (t3) actually routes to opus. Endpoint already env-configurable via `ANTHROPIC_BASE_URL` (Anthropic-Messages-compatible proxies/gateways work). Tests: `api_model_is_tier_specific_and_distinct`, `api_model_honors_env_override`. (Native codegen `__axon_ai_complete` still uses the sonnet default — threading the tier through the native ABI is a follow-on; the interp/asi-runtime path routes correctly.) **LIVE TOKEN-COUNT METERING DONE**: a LIVE `ai_complete` now charges the meter (and stamps cost_usd) from the model's REAL `usage.{input,output}_tokens` (via `complete_with_model_usage`), not the prompt-length estimate — so metered cost matches what the provider actually billed. The pre-dispatch BUDGET gate still uses the estimate (it must decide before the call). Under mock the count stays the deterministic estimate (reproducible). Test `complete_with_usage_returns_deterministic_token_estimate_under_mock`. Remaining: a compiler-PRIMITIVE `Budget` value type (like `Uncertain<T>`) — note the userland `Budget`/`ResBudget` structs (`examples/stdlib/budget.ax`) AND the kernel `Budget` (carried by `Principal`s; debited by the kernel `Goal`/`LlmGateway`, R12b/fdbb8b2) already exist, so what remains is only the language-level value-type sugar; native-codegen tier threading. | 📝 `governance/specs/R3-ai-primitive.md`, `R3b-named-tier-arg.md`, `R3c-ai-budget-meter.md` | Policy-gated fallback fires offline ✅ (E1300); **tier routing pins real model ✅ (live request now sends the tier's model + env override)**; **per-call `tier:` override ✅**; **AI call-budget enforced ✅** (E1301; per-fn `budget: N`); **per-token cost metered ✅** (real cost_usd + ai_cost_spent); **ai_call provenance recorded ✅** |
| **R4** | Three code zones: Static / Adaptive / Agent + compiler-enforced provenance | Three Code Zones; Error/Perf Logging | 100 | ✅ Strong | `@[adaptive]`/`@[experiment]`/`@[agent]` all behaviorally distinct now. `@[experiment]` injects tagged provenance (`zone:"experiment"`+label) but is excluded from `goal_run` best. **`@[agent]` MANDATORY ACTION-LOG DONE**: `call_builtin` injects an `event:"agent_action"` record (`zone:agent`, `action`=tool, `caps_used`=capability) whenever a capability-bearing builtin runs inside an `@[agent]` fn — compiler-injected, un-opt-out-able (I-13); non-agent fns doing the same call log nothing. JSONL carries real `event`/`zone`/`label`. **CODEGEN PROVENANCE TRIPWIRE DONE** (now R1 native builds): `scripts/provenance_parity.sh` + `codegen_provenance_matches_interp_on_adaptive_returns` compile one `@[adaptive]` program both ways and assert the native binary's return records carry the SAME discriminating fields (`event:"adaptive_return"`/`zone:"adaptive"`/`fn`/`score`) the interpreter writes. **Found+fixed a real I-13 codegen hole**: native `__axon_provenance_log_ret_{i64,f64}` was writing a degraded `event:"event"` record with no `zone` — fixed in `axon-rt/provenance.rs`. **NATIVE `input` FIELD DONE** (was a residual): native `adaptive_return` records now carry the structured `"input":<n>` field matching the interpreter (via F11's `__axon_provenance_log_ret_i64_in` → `log_adaptive_return(.., Some(input))`), not buried in the payload string. `goal_input_parity.sh` asserts `"score":84,"input":3`. **NATIVE `src` FIELD DONE** (closes the input/src residual): codegen emits `__axon_set_provenance_source(path)` in main's prologue (Codegen `source_path`, set from the build's source path); axon-rt stores it and `log_adaptive_return` stamps `"src":<path>`. Native adaptive records are now BYTE-IDENTICAL to the interpreter's (event/zone/payload/score/input/src), modulo ts_ms. **AGENT-ACTION CODEGEN PATH DONE (last residual closed)**: native codegen now injects the mandatory `agent_action` log too — codegen tracks `current_agent_fn` (set on `@[agent]` fn entry) and emits `__axon_log_agent_action(fn, action, caps)` at every capability-bearing builtin call inside it (I-13, un-opt-out-able); a non-agent fn logs nothing. Native records are byte-identical to interp (verified: `exec`→`exec`, `read_file`→`fs:read`). **R4 COMPLETE.** | `ROADMAP.md` §6–7; ✅ `governance/specs/R4-code-zones.md` (Reviewed) | Adaptive fn cannot compile without provenance injection ✅; `@[experiment]` distinct ✅; agent action log mandatory ✅ (cli_run: agent_actions_are_mandatorily_logged); **codegen↔interp provenance parity ✅** (adaptive byte-identical incl. input+src; **agent_action log too** via agent_action_parity.sh) |
| **R5** | Goal-directed optimization — `#[goal]` type, 3 strategies, deterministic test sets, eval hierarchy | Autonomous Optimization | 94 | ⚠️ Partial | **`#[goal(strategy: …)]` ATTRIBUTE DISPATCH DONE (PRD L889-899)**: the sugar now accepts a `strategy:` key dispatching to the closed set `hill_climb` (default) / `random` / `multistart` / `tournament` / `bayesian`, with `lo:`/`hi:` search-box keys; an unknown strategy is **E1505** at check. **`tournament` is a real generational search** (`run_goal_tournament`): sample a population in `[lo,hi)`, keep the top-K elites, breed by mutating around them with a step that halves each generation (coarse→fine, elitism preserved). **`bayesian` is the honest exploit-biased mapping** (single elite + heavy local refinement — NOT a GP surrogate, documented as such). All five reach the peak deterministically under `AXON_SEED` (tests `goal_strategy_attribute_dispatches_all_five`, `goal_unknown_strategy_is_e1505`). Runtime strategy builtins still available directly (`goal_run`/`_continue`/`_random`/`_multistart`); **eval hierarchy STARTED**: `goal_eval(name, input)` does HELD-OUT evaluation — runs the metric on a held-out point WITHOUT recording it as a training probe (snapshot+restore, no overfitting/no provenance pollution), the train→holdout→target-gate primitive. **`#[goal(metric, target, max_evals, holdout)]` ATTRIBUTE SUGAR DONE**: auto-wires goal_run (train) + goal_eval (held-out) + a gate, injecting `goal_met` into the fn body; E1500/E1503/E1504 validation. **FIRST-CLASS GOAL VALUE (userland) DONE**: `examples/stdlib/goal.ax` — a `Goal` *value* you construct/store/pass/evaluate (the ROADMAP §6 `Goal<M>` shape at the userland layer, same two-track move as agent.ax/F12), bundling metric+target+Budget+a hard Constraint; `goal_observe` marks it met only when the target is reached AND the hard guard holds (a high score that breaks the guard is disqualified to 0 — safe optimization), `goal_step`/`goal_can_step` enforce the spend budget. 6 @[test] gated (cli_run `goal_stdlib_module_tests_pass`). **LIST-LITERAL `test_set: [...]` DONE**: the attr parser now accepts a bracket list (`parse_attr_atom` renders `[a,b,c]` → flat `"a,b,c"`); `#[goal(... test_set: [5,7,9])]` evaluates the metric on EVERY held-out point and gates on the WORST (min) score — so a fn cannot pass by overfitting one point; a non-integer element → E1503. Still missing: kernel `Goal<M>` value type with SMT-checked constraints + principal scoping (Phase-7). | `ROADMAP.md` §5–6; `spec/stdlib.md`; `examples/stdlib/goal.ax` | `#[goal(metric, test_set, target)]` runs to target on a held-out set ✅ (multi-point worst-case gate); provenance per experiment ✅; **first-class Goal value (userland) ✅**; `#[goal]` attr sugar + `test_set: [...]` ✅ |
| **R6** | Capability security — content-addressed imports, compile-time I/O restriction, AI audit on import | Supply Chain Security; Capability Permissions | 91 | ⚠️ Partial | `@[contained]` enforces net/fs/exec at fn boundary. **Import-edge boundary VERIFIED (I-11)**: static cap checker catches a `mod`+`use`-imported fn's `@[contained]`/`never:` violation (E1004) on the merged program. **Content-addressed lockfile WORKING end-to-end**: `lockfile.rs` (zero-dep `axh1:` SHA-256 + deterministic `axon.lock`) wired to CLI — `axon lock <file>` hashes each `use`d module + writes the lock; `axon verify-lock <file>` recomputes and rejects a tampered module with **E1201** (exit 2), a missing entry with **E1202**. The PRD's "tampered content hash rejected" is delivered. **Import-edge cap-widening DONE (E1203)**: a `@[contained]` importer that imports a module exercising a capability it doesn't grant is rejected at `axon check` (`check_import_capabilities`, opt-in: uncontained importers unaffected — back-compat). **Transitive-`use` hashing DONE**: `axon lock`/`verify-lock`/E1203 now cover the whole import closure (prog→mid→leaf all pinned; a tampered deep module is E1201). **AUDIT-ON-IMPORT DONE (E1204)**: `audit.rs` — `axon lock` audits each module's capability surface (`audit_module`) and pins a deterministic verdict (`clear`/`flagged`/`denied`) into `axon.lock` as `<verdict>:<hash>`; on every build the verdict is re-validated by hash (**no AI call at compile time**); a `denied` verdict blocks the build with E1204. **`exec` BUILTIN + EXEC CAPABILITY DONE (closes the deviation)**: a real `exec(cmd, [args]) -> Result<str,str>` builtin spawns a process via the `AxonHost` seam (DefaultHost = std::process; a sandboxed/browser host defaults to denied); `classify_call` maps it to `IoKind::Exec`, activating the previously-dormant `@[contained] exec: none|any` axis — `@[contained(exec: none)]` now rejects `exec` at check (E1001), `exec: any` allows it. The R6 audit now keys `denied` on undeclared **`exec` OR `net`** (both highest-risk surfaces), matching the spec instead of substituting net. **CODEGEN `exec` EMITTER DONE**: `exec` is no longer interp-only — native codegen emits it (`__axon_exec` in axon-rt: spawns the process, returns stdout/err via the ±len Ok/Err convention; codegen unpacks the `[str]` args array). Native==interp on both the Ok (stdout) and Err (message) paths (`scripts/exec_parity.sh`); previously native silently produced no output. **`@[sensitive]` DATA-SOVEREIGNTY GUARD DONE (PRD §4 / Critical-Considerations CC4)**: a `@[sensitive(category)]` struct type (`pii`/`phi`/`financial`/…) may not flow into an external AI call — `ai_complete`/`ai_extract_*` with a sensitive-typed argument is **E1206** at check, naming the category + type. The checker collects sensitive type names (`TypeDef.attrs`, new AST field threaded through the parser) and flags a sensitive `Type::Struct` argument to an AI sink; local use of the value is unaffected (no false positive), and a plain struct is allowed. The boundary is **exfiltration in general** — an AI call, `write_file` (to disk), OR `exec` (to a process) — via `exfiltration_sink_kind` (the message names which boundary). Catches a whole sensitive struct, a **field of a sensitive struct** (`u.email` — PRD's "can't exfiltrate sensitive fields"), a **field whose type is sensitive** (`w.user`), and a let-bound sensitive value. Tests: `sensitive_type_into_ai_call_is_e1206`, `sensitive_field_into_ai_call_is_e1206`, `sensitive_type_used_locally_is_allowed`, `non_sensitive_type_into_ai_call_is_allowed`; `examples/sensitive_data.ax`. Remaining: full transitive taint (a sensitive value laundered through an arbitrary expression/helper fn) + auto-`.to_redacted()`. Remaining: AI-audit *quality* layer (obfuscation reasoning via R3) + transitive sensitivity taint. | 📝 `governance/specs/R6-capability-security.md` (Reviewed) | Import without declared capability fails compile ✅ (E1004 + **E1203 import-edge**); **tampered content hash rejected ✅** (axon lock/verify-lock → E1201); **audit on import ✅** (E1204; exec OR net denied; deterministic verdict pinned + hash-revalidated); **exec capability live ✅** (exec builtin; E1001 on `exec: none`); **`@[sensitive]`→AI is a compile error ✅** (E1206, PRD §4 privacy) |
| **R7** | Cross-platform targets — native / wasm / js / mobile from one source | Cross-Platform; Mobile; UI; 3D | 89 | ⚠️ Partial | **Slice A DONE — Axon runs on wasm.** The tree-walking interpreter compiles to `wasm32-wasip1` and runs `.ax` headless under `wasmtime` with **identical exit codes + stdout to native on **28/28 pure-compute examples (AUTO-DISCOVERED** — every fn-main example touching no host interface, not a hand-curated subset)** (same `interp.rs`, two targets — identical by construction, I-2; verified live this session). One host touchpoint changed: `on_deep_stack` runs on the single wasm stack (`#[cfg(wasm32)]`); 64 MiB wasm stack + `RECURSION_LIMIT=450` so deep recursion fails with the same graceful panic as native. **`axon target` CLI + AxonHost SEAM DONE**: host touchpoints route through a thread-local `AxonHost` (DefaultHost==std on native). **Slice B AOT-wasm OBJECT EMISSION DONE**: `axon target build --engine codegen --target wasm32` emits a real WebAssembly object via the inkwell wasm32 backend (`compile_to_wasm_object`), magic-verified `\0asm` (confirmed `file`-recognized 22 KB MVP wasm from math.ax) — the real IR→wasm codegen step, no longer an E0907 stub. **Slice B link FULLY DIAGNOSED end-to-end (2026-06-03)**: built `axon-rt` for `wasm32-wasip1` (succeeds, 11 MB staticlib), emitted the object for wasip1, and linked object + wasi `libc.a` + `crt1-command.o` + `libaxon_rt.a(wasm)` via `rust-lld -flavor wasm`. The link SUCCEEDS-with-warnings but the wasm **TRAPS under `wasmtime`** — `rust-lld` reports `function signature mismatch` on `__axon_str_*`/`__axon_parse_int_err` AND even `memcmp`/`write`/`strlen`/`snprintf`: codegen bakes **i64** ptr/len into the str/array IR (the AxonStr `{i64 len, i64 ptr}` ABI) while wasm32 needs **i32**. Even `21+21` drags all 19 `__axon_*` externs (`declare_builtins` emits the whole table). So the real Slice B blocker is a **wasm32 codegen ABI retarget (i64→i32 pointers + wasi entry + extern pruning)** — multi-slice codegen work, scoped in `R7-targets.md` §12 Q6, NOT a one-step link. The libc/sysroot half is solved (wasi `libc.a` + a building wasm axon-rt are on disk). **Slice A (interp→wasm, 28/28 parity) remains the shipped path satisfying the R7 "runs identically" acceptance by construction.** **FILE-I/O PARITY DONE (cross-platform extends past pure-compute)**: read_file/write_file route through the AxonHost seam (DefaultHost=std::fs), and WASI provides std::fs under a capability grant — so a write+read round-trip is BYTE-IDENTICAL on native and wasm32-wasip1 (`scripts/wasm_fs_parity.sh`, `examples/file_roundtrip.ax`). The capability model carries over (wasm touches only `--dir`-granted paths). **env vars too**: `env_var` reads WASI `--env` the same as `std::env` on native (byte-identical, `wasm_fs_parity.sh` covers both fs + env). So THREE host-interface dimensions — compute, fs, env — are all proven cross-platform on the shipped interp→wasm path.** **AOT-wasm — first codegen slice DONE (dead-function pruning)**: `declare_builtins` emitted ~119 helper fns unconditionally, dragging all 19 i64-ABI `__axon_*` externs into even `21+21` — the wasm `function signature mismatch` at link. `compile_to_wasm_object` now runs `prune_dead_functions` (delete every body-bearing fn with zero uses, to a fixpoint; main + address-taken fns kept) so a pure-int wasm object has **0 `__axon_*` imports** and `rust-lld` links it with **0 signature mismatches** (`scripts/wasm_object_prune.sh`). Native UNCHANGED (pruning is wasm-path-only; it exposed a latent native libm `pow` link-order fragility, so it's not applied there) — native==interp parity stays 32/32. Remaining AOT-wasm: the wasi ENTRY-POINT ABI (crt1's `__main_void` weak-links `main` with a signature codegen doesn't yet emit for wasm) — the next sub-slice; then str/array i64→i32 for non-pure-int programs.**the next sub-slice; then str/array i64→i32 for non-pure-int programs.** **AOT-WASM NOW RUNS (pure-int) — first runnable codegen target beyond native**: the entry-point sub-slice landed via REACTOR-mode linking — `axon target build --target wasm32-wasip1` links the pruned object with `rust-lld -flavor wasm --no-entry --export=main` against the wasi libc and the result RUNS under `wasmtime --invoke main` with the SAME value as the interpreter (fib(10)=55, arith=80 — `scripts/wasm_aot_run_parity.sh`, cli `wasm_aot_runs_and_matches_interp_on_pure_int`). A str/array program (surviving i64-ABI extern) correctly reports object-only, not a false 'runnable'. So AOT-wasm execution works for the pure-compute subset; the remaining gap is the str/array i64→i32 ABI retarget for the rest. Native untouched (32/32 parity).** **AOT-WASM NOW RUNS THE FULL DETERMINISTIC SURFACE (2026-06-10) — the "i64→i32 ABI retarget" was a MISDIAGNOSIS.** The actual blocker was never a deep pointer-width retarget: the `AxonStr` array layout agrees by construction (both sides malloc `size_of::<AxonStr>()`-stride, codegen reads with the target {i64,i32-ptr} struct). It was simply MISSING `#[cfg(target_arch="wasm32")]` extern variants. Ported them all — str (incl. `str_split`/`str_join`, array-of-str), the FULL dict API (all 17 `__axon_dict_*`, incl. closure ops map_values/filter/each via a scalar-expanded transmute), with the AxonStr-by-value args expanded to (i64 len, i32 ptr) scalars matching codegen's wasm call ABI. Audit confirms **0 of 69 externs are native-only** now. Result: **AOT-wasm runs 30/30 deterministic `examples/*.ax` byte-identically to the interpreter** (`scripts/wasm_examples_parity.sh`, cli `wasm_examples_run_identically_on_aot_wasm`, FLOOR=25), plus `wasm_aot_run_parity.sh` 7/7 (str/array/f64/loops/full-dict) and `wasm_str_abi_parity.sh` 13 builtins. Native untouched throughout (every change is wasm-isolated; the `_impl` refactors are behavior-preserving — dict_parity green, 70 axon-rt tests pass). **BROWSER TARGET (R7c) NOW RUNS THE FULL DETERMINISTIC SURFACE (2026-06-10).** `axon target build --target wasm32-unknown-unknown` emits a genuinely **WASI-FREE** module (the link was target-aware'd: unknown-unknown axon-rt + no wasi libc); browser-only shims (`malloc`/`free`/`puts`/`write`/number-format externs, all `#[cfg(target_os!="wasi")]`) make compute, str, dict, AND complete printing (str/int/**float**/interpolation) work. **AOT-wasm now runs 29/29 deterministic `examples/*.ax` on the browser target** byte-identically to the interpreter (`scripts/wasm_browser_examples_parity.sh`, driven by a Node host standing in for the page glue), plus a real openable demo (`examples/browser/` — index.html loads the module with one host import `axon_host_write`). Codegen now emits **no libc `snprintf`/`strtoll` at all** (to_str/to_str_f64/parse_int → axon-rt externs). The only browser exclusions are genuinely host/time/non-deterministic (AI, randomness, now_ms/temporal, I/O, goal). **The Phase-6 `resume` runtime now has a WORKING v0** (spec `R15-resume-runtime.md`, `623d1a2`/`f71d75e`): `host_await(req)->reply` genuinely SUSPENDS the program and resumes with the host's reply — a worker-thread substrate (the `!Send` interp stays put; str payloads cross as `String`), B1/B2/B3/B4/B5 green (B2 = effects fire exactly once, proving non-replay). Usable via `axon run` (stdin/stdout host) — interactive CLI programs (a prompt loop, a human-in-the-loop approval agent — `examples/interactive/`) work today. This **partially lifts the interactive gate**: NATIVE/headless interactive (R13/R14's runtime layer) is unblocked at the suspension level (they still need a UI/windowing host). Remaining: the BROWSER binding (single-threaded → Asyncify or a JS step-loop; R7c) and arbitrary-`Value` payloads (a same-thread stackful coroutine, or a safe deep-clone-to-`Send` for native) — both focused efforts; plus real wasm-bindgen DOM/canvas/WebGPU and js/mobile targets. | 📝 `governance/specs/R7-targets.md` (Reviewed — Slice A + object-emit landed; Slice B link fully diagnosed = ABI retarget) | Same `.ax` runs on native and wasm with identical observable results ✅ (**28/28 pure-compute, auto-discovered**, `scripts/wasm_parity.sh`); **AOT wasm object emitted from codegen ✅** (magic-verified); **AOT wasm link diagnosed: i64↔i32 ABI mismatch is the real blocker, not libc ✅** (empirically traps under wasmtime) |
| **R8** | Built-in testing + structured errors — `#[test]`, property-based `forall`, AI-parseable diagnostics | Testing; Error Messages | 100 | ✅ Strong | `@[test]`/`should_fail` + **`forall` property testing DONE**: `@[test] @[forall(n: N)]` randomizes typed params (i64/f64/bool/str) over N cases via seeded RNG, and on failure BINARY-SEARCH-SHRINKS to the minimal counterexample (`a < 50` → `a=50`) with a reproduce seed. **Versioned machine-stable diagnostic schema DONE** (`axon-diag/1`, `diag_schema.rs`): `axon check --json` emits structured `{schema,severity,code,message,help}` NDJSON (code as a first-class field, version-tagged). **TYPED END-TO-END DIAGNOSTIC DONE** (§10): `run_check_pipeline_located` resolves each typed error's byte-`Span` → (line,col) via `SourceMap` and returns located `PipelineDiagnostic`s; `PipelineDiagnostic::json()` emits `axon-diag/1` WITH `file`/`line`/`col` first-class; `axon check --json` now routes through it (E0102 mismatch → `"line":3,"col":5`). **STRUCTURED `expected`/`found`/`help` FIELDS DONE**: `PipelineDiagnostic` now carries `expected`/`found`/`help: Option<String>` (from the typed InferError/CheckError), and `json()` emits each as a discrete key when present — so a tool can branch on the type pair without re-parsing prose (`{...,"expected":"str","found":"i64"}`). Additive (consumers ignore unknown keys), so the schema stays `axon-diag/1`. **PARSE-ERROR LOCATION DONE** (last residual closed): `Parser::parse_program_located` captures the failing token's `current_span`; `parse_source_located` returns the byte offset; `cmd_check` resolves it to (line,col) and emits a located `PipelineDiagnostic` JSON — a parse error like `@` now reports `"line":2,"col":5` instead of span-less (lexer errors map to line 1 col 1, carrying their own position in the message). **R8 COMPLETE.** | `TESTING_STANDARD.md`; `spec/stdlib.md`; `governance/specs/R8-diagnostic-schema.md` | `forall(a,b){assert(f(a,b)==f(b,a))}` runs N cases ✅ + shrinks ✅; error JSON schema versioned ✅; **diagnostics carry source location end-to-end ✅** (incl. parse errors); **structured expected/found/help ✅** |
| **R9** | Layer-1/3 alignment — `Uncertain<T>`, `Temporal<T>`, `#[contained]`+`#[corrigible]`, `#[verify]` | ASI Extensions; Structural Alignment; Formal Verification | 88 | ⚠️ Partial | Uncertain/Temporal/`@[verify]`(runtime, composite preds)/`@[contained]` done. **std.causal (userland do-calculus) DONE (PRD "std.causal — Causal Reasoning", was 0%/prose)**: `examples/stdlib/causal.ax` — a structural causal model with `observe`/`do`/`counterfactual` showing the PRD's whole point: under a confounder the OBSERVATIONAL effect OVERSTATES the true CAUSAL lever (a `#[goal]` without a causal model chases the inflated correlation — causal effect 2 vs naive correlation 3). 5 @[test] (`test_do_and_observe_disagree_under_confounding` + agree-without-confounding, lever-invariant, counterfactual-holds-season), gated by `causal_stdlib_module_tests_pass`. The `causal.model{nodes,edges,confounders}` literal + `#[goal(causal_model:)]` need language sugar — userland module is the semantic core (same two-track move as goal.ax/principal_mint.ax). **TEMPORAL<T> CONFIDENCE DECAY DONE (was doc-vs-code divergent)**: `Temporal` now carries a `confidence` field (1.0 at creation) and `temporal_at(t, offset)` applies the documented EXACT decay `confidence * (1 - decay)^(offset_ms / 86_400_000)` — the time-awareness the type exists for. The interp was BEHIND (4-field struct, no confidence, no decay) AND codegen used a *linear* approximation; this brings interp to codegen's 5-field layout and upgrades codegen to the exact power form (via `llvm.pow.f64`), so native==interp byte-identical (verified: +30d at 2%/day → 0.545, value unchanged). New `temporal_confidence(t)` accessor (`.confidence` field also works). `examples/temporal_decay.ax` (in the 31/31 parity sweep), test `temporal_at_decays_confidence_over_time`. **AGENT METACOGNITION (builtin subset) DONE (PRD "Agent Metacognition")**: an agent can inspect its own reasoning trace to catch its own failures — `agent_trace_len(name)` (# reasoning steps), `agent_uncertainty(name)` (self-estimated uncertainty in [0,1] from score spread; 1.0 with <2 records), `agent_detect_loop(name)` (has progress stalled? last-3 scores within epsilon → likely loop). All read the same in-memory provenance `goal_history` exposes (deterministic, interp-only like the goal-introspection family). Verified: a flat @[adaptive] fn → detect_loop true + uncertainty 0; no trace → uncertainty 1.0 + 0 steps. `examples/asi/agent_metacognition.ax` (metacognitive control flow), test `agent_metacognition_reads_its_own_trace`. v1 is the trace-backed CAPABILITY; the PRD's `self.method()` trait-on-mod surface (ReasoningStep/BlindSpot types) is the follow-on. **UNCERTAIN<T> BINOP EXECUTION IN THE INTERPRETER DONE (was a real gap)**: codegen had `emit_binop_uncertain` but the interpreter's `eval_binop_vals` did not — so confidence-propagating code (`uncertain(10,0.9) + uncertain(5,0.8)`) could be compiled but PANICKED when interpreted (`axon run`). Fixed: `eval_binop_vals` now detects an `Uncertain` operand, operates on the inner values, and carries `min(confidence)` forward (a non-Uncertain side = confidence 1.0; comparisons yield `Uncertain<bool>`) — byte-identical to codegen (`uncertain_propagation.ax` runs the same native+interp, in the 29/29 parity sweep). `&&`/`||` over `Uncertain<bool>` route through the same path (no short-circuit when combining confidences). Tests: cli `uncertain_binops_propagate_minimum_confidence`. **`@[corrigible]` LATCHING KILL-SWITCH DONE**: `corrigible_halt()` trips a one-way latch (no resume builtin by design); once latched, every `@[corrigible]` fn call is refused before its body runs (engine-enforced, keyed on the annotation) — `corrigible_halted()` lets a loop wind down gracefully, an unguarded post-halt call fails closed with a distinct exit code 4 (`Flow::Halted`, distinct from panic 101 / verify 3 / static 2). The latch is targeted (non-corrigible fns still run). **SMT-backed `@[verify]` DONE** (`smt` feature, opt-in): `axon verify` encodes a fn's body as a Z3 term and PROVES the `value OP K` bound for all inputs (unsat negation) or reports a concrete counterexample (E1102, e.g. `dec(x)=x-1` violated at x=0); out-of-fragment (loops/calls) → W1103 (runtime gate applies). **FLOAT FRAGMENT DONE**: `f64` params/returns/non-integer bounds now route to a Z3 **Real** encoder (`prove_one_f64`/`encode_expr_real`) — `axon verify` PROVES float bounds (`x*x >= 0.0` proven for all real x; `x-0.5 >= 0.0` → counterexample) over straight-line `+`/`-`/`*`/`ite`. Honest caveat: Z3 Real is exact rational arithmetic, so it proves the *mathematical* bound (IEEE rounding/NaN/Inf out of fragment). **COMPOSITE (CONJUNCTION) PREDICATES DONE**: `axon verify` now statically proves a `value`-conjunction like `@[verify(value >= 0 && value <= 100)]` — `decode_verify_conjunction` flattens the `&&` chain into atoms, the SMT prover conjoins them and sat-checks `¬(A₁∧A₂∧…)` (unsat→all proven; sat→one counterexample naming the full predicate). A `confidence` atom or a `||` disjunction → Unsupported (the runtime gate, which already handles arbitrary boolean predicates, enforces it) — never a false proof. Tests: `smt_proves_a_value_conjunction`, `smt_conjunction_counterexample_on_one_atom`, `smt_confidence_conjunct_is_unsupported_not_false_proof`, `smt_disjunction_stays_unsupported`, + `decode_verify_conjunction` unit tests. Straight-line integer + float fragments done; loops/recursion-invariants remain (need invariant inference, R9b spec). Z3 isolated behind the feature — default build never links it (verified by the standard gate). | `spec/compiler-phase5.md` (SMT); `ROADMAP.md` §0,§7 | `@[corrigible]` kill-switch latches ✅; **`#[verify]` proven by Z3 on integer AND float bounds ✅** (smt: 8 unit; `axon verify` proves x*x>=0.0) |
| **R10** | Self-improving compiler — learned optimization passes graduated from AI-discovered asm | Self-Improving Compiler; Intelligence Maturity | 99 | ⚠️ Partial | **Verification harness + graduation firewall BUILT.** Four gates (`improve.rs`): G1 correctness (interpreter oracle, whole corpus, E1401; never AI — E1406 by construction), G2 capability-diff (I-12, E1402), G3 regression (`@[test]` outcome preserved, E1403 — catches what G1 misses), G4 perf (opt-in timing, advisory `faster` claim, W1410). Graduation (`manifest.rs` + `axon improve` CLI): `graduate` enforces multi-sig of ≥2 **distinct** root Principals (E1404 — a duplicate signer / self-graduation is refused); content-addressed `passes.manifest` (`axp1:`/`axv1:`/`axc1:`), malformed → E1405; a pass not in the manifest never runs; `list`/`revert` round-trip. **DISCOVERY (unprivileged proposal side) BUILT**: `axon improve discover <corpus>` scans the corpus for the canonical safe optimization — arithmetic-identity simplification (`x+0`/`0+x`/`x-0`/`x*1`/`1*x` → x) — via a deterministic read-only AST walk (`improve::discover_arith_identities`), and writes a `proposals/<name>.proposal` that **grants nothing** (pure data: name/description/opportunity-count). Only `verify` (four gates) then a multi-sig `graduate` can make a pass runnable — the I-12 firewall (discovery proposes, humans dispose) holds by construction. A clean corpus proposes nothing (no vacuous pass). **REAL REWRITE PASS BUILT + VERIFIED**: `improve::fold_arith_identities_pass` is the executable counterpart — a pure `Program→Program` rewrite that folds `x+0`/`1*x`/etc. to `x` recursively (conservative: only collapses provable no-ops, returns unlisted variants unchanged). `axon improve verify --pass fold-arith-identities` runs the ACTUAL rewrite through the four gates and PASSES G1 (the interpreter oracle confirms behavior is preserved) + G2 (no new capability) over **all 28 examples** — a real, proven-correct optimization, not the identity baseline. The discover→verify→graduate pipeline is now end-to-end with a real pass. **AI-DRIVEN DISCOVERER BUILT (the PRD headline `axon improve discover --ai`)** — discovery is AI-driven WITHOUT letting AI weaken the TCB: the AI selects template NAMES from a **closed compile-time registry** (`improve_templates::TEMPLATES`, `const [(name, fn-ptr)]`); it NEVER authors code. An unknown name → **E1407** before a proposal is written (never reaches verify); a graduated name absent from the registry → **E1408** (tamper/skew). The selected name maps to the SAME deterministic Rust pass the four gates verify, so an AI selecting a (hypothetical) miscompiling template still fails G1 — the firewall is unchanged, AI cannot route around the interpreter oracle (I-2/I-12 hold by construction). Discovery origin (`proposed_by: static|mock|ai:<model>`) round-trips Proposal→manifest so a multi-sig reviewer sees AI provenance. Deterministic under `AXON_AI_MOCK=1`. A 3-lens design + **5-attack adversarial red-team (multi-agent workflow)** preceded the build; 2 real holes (TOCTOU on name validation, provenance-audit gap) were closed by construction. **LIVE-MODEL WIRING DONE**: `axon improve discover --ai` now routes the menu+digest prompt to a real model under `--features asi-runtime` (+ ANTHROPIC_API_KEY) via `complete_with_model`, recording the model id for audit (`proposed_by: ai:<model>`); the default build prints an honest feature-required message; mock stays deterministic. SAFE BY CONSTRUCTION — the live reply is validated identically against the closed registry (unknown name → E1407) and a selected template still runs the four-gate firewall + multi-sig graduation, so a live or compromised model gains nothing. Remaining: growing the template menu (each a reviewed commit). | 📝 `governance/specs/R10-self-improving-compiler.md` (Reviewed) | A discovered pattern verified correct (G1 ✅) + safe (G2 ✅) + non-regressing (G3 ✅) + faster (G4 ✅), graduation gated by multi-sig (E1404 ✅); **discovery proposes candidates ✅**; **real rewrite pass verified correct+safe ✅** (fold-arith-identities through the gates); **AI-driven discoverer ✅** (closed-registry selection, E1407/E1408, proposed_by provenance, mock-deterministic, red-team-hardened) |
| **R16** | GPU-rendered UI framework — **Axon UI** (2D + 3D): one `fn app(s)->View` → native (winit+wgpu) / browser (WebGPU canvas) / mobile / future Axon OS | UI — GPU-Rendered, Not Webview; Mobile; 3D — Scene Graph on wgpu | 0 | 📝 Spec drafted (Draft); not started | **The whole framework is greenfield — spec only, no code.** The spec freezes the *architecture & contracts* (`View`/`Msg`/`update` Elm model, taffy layout, the reactive frame loop, the new `Gfx` capability axis, the `GfxHost` seam, the I-2 pixel-parity amendment) but nothing is built. Interactivity is **gated on R15** resume runtime (landed). v1 = a **2D vertical slice** (the `users-list` demo widget set only — the catalog is demand-driven *by design*, §3a); 3D is Phase-4 (a Three.js-subset). **Hard deferrals named, not built**: text/font shaping, **accessibility (must be designed against the View tree *before* the Slice-0 freeze)**, scroll/virtualization, hi-DPI. The shipped product UI today is `axon-web` (HTML/JS), which already covers the approval flow — so whether Slices 1–5 are "build next" vs. "parked after Slice 0" is an open **strategic** call (spec §12 Q7). | 📝 `governance/specs/R16-axon-ui.md` (Draft) | Slice 0: `axon_ui_renders_static_view_to_golden_png` + `axon_ui_layout_boxmodel_is_deterministic`; Slice 2 (the 2D done gate): `axon_ui_users_list_journey` + native↔browser layout parity |
| **R17** | Freestanding substrate + trusted HAL — **bare-metal Axon** (the low-level escape hatch for a from-scratch Axon OS) | Compiler Architecture; (OS-from-scratch — **reverses ROADMAP §2.3**) | 70 | ✅ **Slices 0–3 LANDED** (founder-committed 2026-06-19; merged to main `4ed9fcd`) | **Slices 0–3 are all landed** (spec Status header + commits `076c445`/`bf97c55`/`76860bc`/`a7b262c`/`4ea3ab5`, verified 2026-07-18): **Slice 0** — `@[entry]`/`@[panic_handler]`, `Hal` effect row, HAL builtins, `axon build --freestanding`; **Slice 1** — real LLVM inline `asm(...)` (E0910 in interp — QEMU is the oracle), hex/binary/underscore literals, `@[naked]`/`@[interrupt]`, `hlt`/`cli`/`sti`, `port_out_u8`/`port_in_u8`, `scripts/kernel.ld` + `--linker-script`, real QEMU serial-hello boot; **Slice 2** — SMP atomics `atomic_{load,store,fetch_add,cas}_i64` with compile-time memory-order literals → real LLVM atomics, golden-IR race-free gate, `axon build --emit-llvm`; **Slice 3** — `@[repr(C)]`/`@[packed]`/`@[align]` byte-exact struct layout + `@[no_alloc]` → E1704 (transitive). The unsigned-int prerequisite this row previously called "just committed" is itself fully landed (R19, see the R19–R34 wave below). **Remaining:** the full 2-core QEMU SMP harness (the golden-IR proxy stands as the unit gate per spec §9) and `axon_kernel_handles_timer_interrupt` IDT/PIC timer wiring (now unblocked by `@[repr(C)]`; deferred to a wiring slice). | 📝 `governance/specs/R17-freestanding-substrate.md` (Slices 0–3 landed) | `axon_kernel_boots_qemu_serial_hello` ✅ (`76860bc`) + `axon_smp_atomic_counter_is_race_free` (golden-IR) ✅ + `axon_repr_c_gdt_layout_byte_exact` ✅; remaining: 2-core QEMU SMP harness + timer-interrupt wiring |
| **R18** | Provenance Ledger — the decision-memory substrate for AI-driven companies (productize the runtime provenance/replay/audit/counterfactual substrate as a causal, retrievable timeline *across* existing systems) | ROADMAP §2.1 (OS = abstractions); §6 stdlib `Trace`/`AuditEvent`/`Counterfactual`/`Principal`/`Store`; §9.5 F2/F3 | 0 | 🟢 **Wedge GREENLIT (founder 2026-06-19)** — validate via the reshaped Slice-0 spike; blocked only on external inputs (Source A repo + ≥3 buyer-jury contacts, founder to supply) | **Not a language feature — a product direction.** Axon already built a single-program version of the "event-sourced company": provenance log (I-13), deterministic **replay** (F2), typed **`AuditEvent`** (F3), **`Counterfactual<T>`**, **`Principal`**, **`Store<T,C,L>`** — the substrate models *decisions* (rationale + knowledge-state + outcome + counterfactual), not just events, which is the gap incumbents (Datadog/PostHog/Drive/Slack) can't retrofit. **What's close to Axon's strengths**: the decision-typing + replay + counterfactual + as-of/causal query layer. **What's greenfield (NOT compiler work)**: the scalable event store, ingestion adapters, UI, multi-tenancy/compliance — the conceptual head start is *not* a product head start. **Beachhead** = the AI-coding-agent timeline (agent-session ↔ diff ↔ deploy ↔ metrics). **No language invariant changes**; I-10/I-13 are the load-bearing assets, I-11 becomes ledger access-control. **Honest caveat**: "rebuild every company" is a graveyard framing — the defensible wedge is a causal decision *layer across* existing systems (ingest, don't replace), and this is a 3rd/4th platform front where focus, not vision, is scarce. R18 ≈ the same bet as the "sandbox-for-AI-code" value wedge from two faces (remember decisions / sandbox actions). | 📝 `governance/specs/R18-provenance-ledger.md` (Draft) | Slice 0 (spike): `ledger_ingests_external_events_into_typed_timeline`; Slice 1 (coding-agent beachhead): `ledger_why_links_agent_session_to_outcome` + `ledger_whatif_counterfactual_over_ledger_state` |

> **Note on numbering:** R1–R10 are the *Top-10 load-bearing* requirements (the matrix's original scope). **R16** (Axon UI), **R17** (bare-metal substrate), and **R18** (provenance ledger) are newly *first-class-tracked* platform-vision / product requirements (their numbers match their spec IDs, continuing the `governance/specs/` R11–R15 sequence); they are **not** averaged into the all-10 figure — they sit in the "full-platform vision" bucket below. The **R19–R34 spec wave** (vertical targets + the safety/attestation stack) is tracked in the compact table in "The R19–R34 wave" section below rather than as full matrix rows. **R36–R38** (2026-07-18 research pass, see below) extend the same platform-vision bucket; **R35 was deliberately NOT assigned** — the "ASI UI" candidate research concluded the fork is a business decision R16 already frames (§12 Q7 there), not a missing document, so no spec was written under that number.

**Weighted completion:** language-core ≈ 93% (R1-6,8-10 avg; R4/R8 at 100, R10 98, R1 95) · all-10-requirements ≈ 90% (R7 cross-platform at 80 is the laggard among the 10; per `MILESTONE.md`) · full-platform vision ≈ 35% — **R17 bare-metal substrate is now at 70** (Slices 0–3 landed: QEMU boot, inline asm, SMP atomics, byte-exact layout), and the **R19–R34 wave** landed most of its scope (R19/R20 language work; R21–R25 vertical targets; the R26–R31 safety/attestation stack — see the wave table below); **R16 Axon UI** and **R18 provenance ledger** remain 0% (spec drafted), and UI/GPU/mobile/3D + the company-data-foundation product remain greenfield. **R36–R38** (below) are fresh Draft-status research specs, 0% built, not yet in the weighted average. **Strategic note:** R16/R17/R18/R36/R37/R38 are now six competing platform fronts (up from three) — R18 (productize the provenance substrate) is still the most on-thesis per ROADMAP §0/§2.1, but R38 (Axon Embedded) is a close second by the same "reuses shipped machinery, reaches non-adopters" logic; the open question is still which is *the* wedge, now across a wider field (R18 §12 Q1).

---

## The R19–R34 wave (verified 2026-07-18)

`governance/specs/` now extends through R34. These are tracked compactly here rather than as
full matrix rows. Each entry below was verified against its acceptance gate and `git log` —
**not** the spec file's own `Status:` header, several of which are stale (flagged below).
Numbers R21–R25 are **dual-assigned**: two parallel build tracks collided and were
reconciled by renumbering (`2a0399d`), so each of those numbers names two independent specs.

| # | Spec | Verified status | Evidence (gate re-run 2026-07-18 unless noted) |
|---|---|---|---|
| R19 | Fixed-width / unsigned integers | ✅ Landed — Slices A (construction surface, E1900), B (width-correct interp arithmetic), C (native codegen parity) | `scripts/unsigned_parity.sh` byte-identical interp==native; commits `8c4f5f2`/`dec6f50` + Slice C per spec Status |
| R20 | SMT capability proofs (mint attenuation) | ✅ Landed — Slices 0–3: O1∧O2 mint proof, `Discharged` wiring + E1610 build gate, relational refinements, TCB attestation (E1611) | spec Status 2026-06-25; commits `09d0d88`→`a75af20`, overflow fix `9aa40cd`; `scripts/smt_mint_parity.sh` |
| R21 | Decimal (exact money, i128 scale-9) | ✅ Landed interp-only (codegen E0910-refused by design) | `scripts/decimal_parity.sh` PASS; commits `c921136`, `a4f79b1` |
| R21 | Axon-OS supervisor (dual-numbered) | ✅ Landed 100% (re-verified 2026-07-18) — spec's own "Draft" header was **stale, now fixed**; `cargo test -p axon-os` 88 tests pass | commits `1e7be26`→`9f12499` ("R21 100%"); runtime capability-ceiling fix `59f84c0` |
| R22 | Domain interop modules (`native::modbus/fhir/fix`) | ✅ Landed interp-side (codegen E0910-refused) | commits `cff0838`, `5cddca1`; least-privilege deny-by-default fix `9dd67bd` |
| R22 | Intent→approve gateway (`crates/axon-intent`, dual-numbered) | ✅ Landed 100% (re-verified 2026-07-18) — spec's own "Draft" header was **stale, now fixed** | `scripts/r22_acceptance_gate.sh` PASS; commits `5e6e2fd`, run-boundary wiring `c7d1d6a` |
| R23 | eBPF target | ✅ Slice 1 landed — kernel-verifier-accepted `.bpf.o` | commit `a0e7a1a`; `scripts/ebpf_verify.sh` |
| R23 | Proof certificates (`crates/axon-certcheck`, dual-numbered) | ✅ Landed 100% (re-verified 2026-07-18) (solver-free checker, Z3 out of the TCB) — spec's own "Draft" header was **stale, now fixed** | `scripts/r23_acceptance_gate.sh` PASS; commits `4d8e2b8`, default-build cert gate `9b08122` |
| R24 | TEE target (SGX via Gramine) | ✅ Landed, 3 slices — E1810 `@[enclave]` type rule; gramine-direct sim run (SKIP-guarded, gramine absent on this host); SGX-DCAP attestation is **CI-only** | spec Status; merged via PR #2 (`708fffc`); `scripts/tee_sim_run.sh`, `.github/workflows/tee.yml` |
| R24 | Defended approval boundary (dual-numbered) | 📝 **Spec only** — no implementation commits | spec commits `bacde3f`, `4a599d0` only |
| R25 | Zephyr RTOS target | ✅ Slice 1 landed — Axon runs as a Zephyr app on ARM Cortex-M3 under QEMU | commit `f57e9d1` (pre-renumber); `scripts/zephyr_qemu_gate.sh` |
| R25 | Information-flow monitor (dual-numbered) | 📝 **Spec only** — no implementation commits (the userland `examples/stdlib/tainted.ax` Tier-2 types are a separate realization, not this kernel-level monitor) | spec commits `bacde3f`, `4a599d0` only |
| R26 | Confidential microVM substrate (`crates/axon-attest`, `axon-vm`) | ✅ Landed — mandatory kernel attestation before every boot | `scripts/r26_acceptance_gate.sh` PASS (all §0 checks); commit `b85bd83` |
| R27 | Corrigibility kill-switch + resource bounds (`crates/axon-os`) | ✅ Landed — latch, coalition ceiling, hash-chained latch ledger | `scripts/r27_acceptance_gate.sh` 19/19 PASS; commit `60e091c` |
| R28 | Capability audit ledger (`crates/axon-audit`) | ✅ Landed — **including the 2026-07-18 fix `0bfa74d`**: `Ledger::open()` now eagerly creates the ledger file (previously a run with zero AI calls produced *no file at all*), and `call_builtin` now audits **every** capability-bearing builtin via `audit_effect_kind()` (fine-grained `capability_of_builtin` fs:read/fs:write/net/exec, falling back to `builtin_effect_row`) — previously only `ai_complete` was ever logged | `scripts/r28_acceptance_gate.sh` PASS (post-fix); commits `33b57a3`, `0bfa74d` |
| R29 | Continuous compliance monitor (`axon-os run --monitor`) | ✅ Landed — denied-effect entry trips the R27 kill-switch | `scripts/r29_acceptance_gate.sh` 24/24 PASS; commit `33b57a3` |
| R30 | Unified ASI deployment safety gate | ✅ Landed — R26→R29 pipeline in one script, JSON report. **Re-verified 2026-07-18: the earlier "NOT re-run to completion — timed out past 4 min" claim was itself stale.** It DOES complete (a few minutes, not a hang) but is genuinely **flaky under host contention**: run once alongside this session's concurrent cargo builds/tests, `acc_a1` (clean-repo run) and `acc_a4` (idempotency — two consecutive runs must match) both FAILED, one run stopping early at UNIT_TESTS/R26_ATTESTATION while an immediately-following run passed all 8 stages. Re-run in isolation (no concurrent build load): **6/6 PASS clean**, both idempotency runs produced identical 8-stage vectors. Root cause not chased further (real-KVM boot timing sensitivity under load, same class as `gate-flakes-under-contention` memory) — treat a red run under heavy concurrent load as a flake, confirm with an isolated rerun before treating as a regression | commits `e498056`/`e7faf7a`; `scripts/r30_acceptance_gate.sh` 6/6 PASS (isolated run, 2026-07-18) |
| R31 | Extended TCB attestation chain | ✅ **Landed** — `axtcb1_ext` chains the R27/R28/R29 component binary hashes into the R26 measurement (`axon-vm attest --extended-tcb`, `axon-vm-report/2`; R26 baseline unchanged without the flag). The spec's own "Draft" header — and, until 2026-07-18, ROADMAP §10.6's "Forward" placement — were both **stale** | `scripts/r31_acceptance_gate.sh` ALL PASS; feat commit `d2d6dd4` (798-line `axon-attest` impl + 12 tests) |
| R32 | Formal corrigibility proof (TLA+/Coq) | ✅ **Landed (2026-07-18)** — genuinely machine-checked. Installed TLC (`tla2tools.jar` direct download) and Coq (`apt install coq` 8.18.0, matching the spec's pin) and ran both for real: TLC passes on both configs (5- and 3-principal), `coqc` compiles `R27Corrigibility.v` clean and axiom-free. **Running the real tools for the first time found and fixed 2 genuine proof bugs** (a stale rewrite target after `simpl` over-unfolded a `pose`-bound local; a `rewrite`-direction fragility replaced with `congruence`) **plus 1 gate-script bug** (the TLC check's failure-grep matched the substring "error" inside TLC's own success message "No error has been found," so every real success was misreported as FAIL) — none visible while the checks were SKIPPED | `scripts/r32_acceptance_gate.sh` 20/20 PASS, 0 SKIPPED — see spec §14 Evidence ledger for the exact fixes |
| R33 | Cross-VM safety quorum | 🚧 **Implementing (re-verified 2026-07-18)** — file-based propose/vote/check CLI + pure `check_quorum` aggregator landed in `crates/axon-vm/src/quorum/` (`ee485e8`); exit 13 QUORUM_BLOCKED / exit 14 QUORUM_ATTEST_FAIL distinctness verified; R26/R31 regression suite unaffected (22/22). Vsock transport, R27 coalition ceiling, `axon deploy` integration still open | `scripts/r33_acceptance_gate.sh` ALL PASS; see spec §14 Evidence ledger |
| R34 | Incremental attestation (rolling hash) | 🚧 **Implementing (re-verified 2026-07-18)** — `ChainStore` + `axon-vm chain-stamp`/`chain-verify` landed (`ee485e8`, evidence backfilled `1dba6a9`); tamper detection (exit 15 CHAIN_VERIFY_FAIL) and `run --chain-stamp` pre-launch chain check both verified. The "Not started" claim was stale | `scripts/r34_acceptance_gate.sh` ALL PASS |

---

## New platform-vision candidates (2026-07-18 research pass)

Four parallel research agents evaluated candidate future directions named in a founder session
(ASI UI desktop+web, Full ASI OS, nano/micro ASI kernel) plus an open survey for others. Each was
told to write a spec **only if the direction is real and not already covered** — a "no" with a
memo is a valid, and in one case the correct, outcome. All three specs below are **Draft, 0% built,
not yet reviewed** — research output, not yet a founder-approved commitment (unlike R17's
founder-committed reversal or R18's founder-greenlit wedge).

| # | Spec | Verdict | Note |
|---|---|---|---|
| — | *("ASI UI" candidate — no number assigned)* | ❌ **No new spec.** | `governance/specs/R16-axon-ui.md` already contains the full desktop+browser architecture and already names the exact fork (§12 Q7): `axon-web`'s thin approval UI already satisfies product-v1; R16's GPU framework is only justified once a customer needs the native-app story. The two serve different audiences (app-builders-in-Axon vs. agent-operators) — unifying them into one PRD would blur that, not clarify it. The gap is a business decision, not a document. |
| R36 | Full ASI OS (`governance/specs/R36-full-asi-os.md`) | 📝 **Draft — convergence spec.** Names three honesty gaps in the existing "run an AI agent on a from-scratch attested OS" pitch: (1) **R17 and R26 do not compose today** — R26's microVM boots `crates/axon-guest-kernel` (bare-metal Rust or a Linux fallback), never R17's Axon-language kernel work, despite R26 §1.1 claiming reuse; (2) the interpreter doesn't yet run as a ring-3 user program under the guest kernel (the live enforcement demo's syscall is issued by the kernel itself at ring 0) — "small TCB" and "agent runs inside" cannot both be claimed today; (3) attestation is a qemu-swtpm software stand-in, not SEV-SNP/TDX. Also found and documented a real bug: `scripts/r26_acceptance_gate.sh` fails on an env-var leak between its own steps (one-line fix, in the spec's S0). Defines slices S0 (truth reconciliation) → S5 (product wrap); S1+S2 (ring-3 + interpreter-inside) are the critical path. Blocking founder question (§12 Q1): is the guest kernel's long-run destiny Axon-language (thesis-pure) or an audited Rust TCB (pragmatic)? | Not yet reviewed/committed — this is R17-style (§2.3) territory: a founder decision, not an implementation task. |
| R37 | Nano/micro ASI kernel (`governance/specs/R37-nano-micro-asi-kernel.md`) | 📝 **Draft — new direction, distinct from R17 and R25.** Verified the combination {Axon-owned kernel} × {constrained MCU hardware, Cortex-M/MPU-class} × {runtime-enforced isolation} is empty: R25 rides on Zephyr (compile-time enforcement only, one flat trust domain at runtime), R17 targets general-purpose x86, and `crates/axon-guest-kernel` (K1–K5) is x86_64/KVM-only and POSIX-syscall-shaped. Proposes an Axon-authored, MPU-based, effect-native microkernel (no POSIX translation layer) for Cortex-M-class hardware, ≤32 KB flash / 8 KB RAM / zero heap, reusing R17's language primitives (`@[entry]`, `asm`, atomics) but sharing no kernel code — a sibling to R17, not "R17 but smaller." Reserved E3700–E3704. | Same founder-pull gate as R17 §12 Q1 — the spec is explicit that opening this phase needs customer/use-case pull, mirroring R17's own discipline. |
| R38 | Axon Embedded (`governance/specs/R38-embedded-agent-runtime.md`) | 📝 **Draft — the survey's top pick among 5 candidates considered.** Package the already-landed interpreter + capability checker (`@[contained]`/E1001) + budget meter (R3c) + audit ledger (R28) + deterministic replay (`AXON_AI_REPLAY`) as an embeddable SDK (Rust crate / C ABI / wasm — building on the existing `axon-wasm` cdylib seam) for products that want "AI builds your automation" features without adopting Axon-the-language: the host registers typed tools with effect rows, the host's model emits `.ax`, the compiler refuses undeclared capabilities before first run. Rejected alternatives: a capability-policy sidecar for other languages (forfeits the compile-time-refusal moat, becomes a commodity agent-firewall), a hosted sandbox cloud (funded-incumbent territory, same bet as this one but with greenfield multi-tenant infra), formal-verification-as-a-service (the SMT tractability is language-shaped, doesn't transfer to arbitrary code), robotics actuator interlocks (regulatory-certification market, not a language problem — viable later vertical, not a wedge). Kill-gated Slice 0 hypothesis, pre-registered: can frontier models emit checking-clean, behaviorally-correct Axon at ≥80% success within ≤3 repair rounds (using the landed `axon-diag/1` structured-diagnostic loop)? If not, the wedge dies regardless of sandbox quality. | Directly comparable to R18 (also "package shipped machinery, reach non-adopters") — see updated strategic note above. |

---

## Reading the gaps as a work queue

Ordered by **(load-bearing × cheapness-to-close)** — the autonomous builder's default priority:

1. **R8 `forall` property testing** — cheap, unblocks deeper testing of *everything else*. Do first.
2. **R5 `#[goal]` first-class** — the autonomy engine is closest to complete; finishing it compounds.
3. **R9 `#[corrigible]`** — small surface, large safety payoff; pairs with existing `@[contained]`.
4. **R3 AI routing + `#[ai(policy)]`** — the differentiation; needs a spec first (forks on reproducibility).
5. **R6 capability/registry** — security gate; spec-first, because under-spec = exfiltration risk.
6. **R1 native build** — ✅ RESOLVED (2026-06-25). Build finishes in ~1.5–2.6s and is at verified interp parity (examples + non-wasm + wasm + fuzz harnesses green); closed by the serde-drop + `build_wrappers.rs`, not the planned `axon-rt` migration (de-scoped). No further effort needed; residual refactors fold into R1b/R1c/R1d.
7. **R7 targets / R10 self-improving** — largest, most speculative; spec now, build now that R1 has landed.

---

## Update protocol

- Any commit that changes a requirement's truth **must** update its `%`, `Status`, and `Gap`.
- A requirement moving to ✅ DONE **must** cite the passing acceptance test in the commit body.
- New PRD features get a row here *before* implementation — if it's not in the matrix, it's not a tracked requirement.
- The weighted-completion line is recomputed whenever a `%` changes.
