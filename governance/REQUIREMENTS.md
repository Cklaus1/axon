# Axon Requirements — Top-10 Traceability Matrix

**Source of truth:** `/home/cklaus/projects/BTask/packages/bcode/AI_Language_Plan.md` (the PRD).
**This file:** maps the PRD's ~50 features down to the 10 load-bearing requirements,
records honest current completion, names the gap, and points at the spec + acceptance
tests that close it.

> **For the autonomous builder:** Before starting *any* feature, find its requirement
> here. If the requirement has no spec, write the spec first (`SPEC_TEMPLATE.md`). If it
> has no acceptance test, the feature is not done until that test exists and passes.
> Update the **Status** and **%** columns in the same commit that changes the truth.

---

## Ranking method

Requirements are ranked by **load-bearing-ness**: how many other requirements collapse
if this one is absent or wrong. R1, R2, R3, R10 are foundational; the rest layer on top.

A requirement is **DONE** only when its acceptance tests pass *and* it holds under
`DEFINITION_OF_DONE.md`. "Partial" means real code exists but the acceptance set is
incomplete.

---

## The matrix

| # | Requirement | PRD § | % | Status | Gap (what's missing) | Spec | Acceptance anchor |
|---|---|---|---|---|---|---|---|
| **R1** | Native compiler pipeline at Tier-1 perf (parse→typecheck→borrow→LLVM→native) | Compiler Architecture; Build Phases | 95 | ✅ Working | **Build SOLVED + codegen green.** Stall was a serde×codegen default collision (`BUILD_RESOLVED.md`); fixed by dropping serde-json from default (`cargo build -p axon-core` = ~3s native compiler). Native==interp on **ALL 28/28 fn-main examples under `AXON_AI_MOCK=1`** (`scripts/all_examples_parity.sh` + cli `all_examples_native_match_interp_under_mock`). **Closed the last 2:** the 2 AI examples used to differ ONLY because native codegen ignored `AXON_AI_MOCK` (axon-ai's `__axon_ai_complete` read `ANTHROPIC_API_KEY` and errored), while the interpreter returned a deterministic stub — fixed by making `ai_complete_inner` honor `AXON_AI_MOCK` and return the interpreter's exact stub (`MOCK_AI_COMPLETE`), so native AI output is byte-identical under mock. (Live, un-mocked AI is inherently non-deterministic and not part of byte-parity by design.) all real codegen crashes/correctness bugs fixed: #40 to_str dispatch, #41 closure-passing ABI + enum==, #42 array-of-struct heap corruption (size_of), #36 random_i64 SIGFPE, #38/#39 str_reverse/replace UTF-8, tuples. **#37 parse_int Err message NOW BYTE-IDENTICAL** (codegen delegates to axon-rt's `__axon_parse_int_err`, echoing the input + radix hint like the interp — closed the last non-AI divergence, `error_handling.ax` now identical). **Perf benchmark DONE** (`scripts/perf_bench.sh`): native AOT is **6.0x faster** than the interpreter across 9 pure-compute programs. **`axon build --release` FIXED** (#43, `-Wl,--allow-multiple-definition`). | `spec/compiler-phase1.md`; `BUILD_RESOLVED.md`; `BUILD_DIAGNOSIS{,_2,_3}.md` | A native binary of `examples/*.ax` runs and matches interpreter output ✅ (**28/28 under mock**, scripts/all_examples_parity.sh) + perf-tier benchmark ✅ (scripts/perf_bench.sh, 6.0x faster than interp) |
| **R2** | Type system + borrow checker (HM inference, no null, Result/Option, 2-mode ownership) | Type System | 90 | ✅ Strong | Edge cases in generic + trait resolution; refinement types (Phase 5) not landed | `spec/compiler-phase{2,3}.md`; `spec/compiler-phase5.md` (draft) | `cargo test` infer/checker/borrow suites + `all_examples_typecheck_clean` |
| **R3** | AI as language primitive — `std.ai`, model routing, `#[ai(policy)]`, deterministic fallback | AI as Primitive; AI Policy | 85 | ⚠️ Partial | **AiCall provenance record DONE** (§4.3): every `ai_complete` appends one `event:"ai_call"` NDJSON record (tier/model/params_hash/**prompt_hash SHA-256, not verbatim**/mode/reason/cost_usd), deterministic (R9 replay key). **OFFLINE FALLBACK + E1300 DONE** (§3.3/§6): `@[ai(policy(fallback: "x"))]` parses (nested-group attr form + flat form) and, when offline, returns `Ok("x")` stamped `mode:"fallback"` — the program stays TOTAL; no fallback offline = coded **E1300** (not a generic panic). **TIER ROUTING (policy+default) DONE**: `ai_routing.rs` (cheap/balanced/strong → concrete model+version host table); `@[ai(policy(tier: X))]` resolves the tier (default balanced), and the AiCall provenance now records the *real* routed `(tier, model, version)` instead of a hardcoded "balanced"/"mock" placeholder. Unknown tier → E1302; AI call with no `@[ai(policy)]` → W1310 (un-metered warning). **PER-CALL TIER DONE** (§R3b): `f(args, tier: cheap)` — a scoped additive `Expr::Call.tier` field (NOT a general named-arg grammar); resolution is per-call > policy > default; unknown per-call tier → E1302; codegen ignores it (interp-side AI routing). `axon ai policy` CLI **DONE** (prints resolved tier/model/fallback per @[ai] fn). **BUDGET METER + E1301 DONE** (§R3c, first concrete Phase-7 `Budget`): `@[ai(policy(budget: N))]` meters every `ai_complete` the fn makes and halts the (N+1)th with E1301 before any model dispatch; per-activation (each call to a budgeted fn gets a fresh N via `AiBudgetGuard`); `budget: 0` blocks the first call; no budget → unmetered (back-compat); malformed → W1311 + unmetered. **PER-TOKEN COST METER DONE** (Phase-7 kernel `cost_meter`, closes ROADMAP F4): `ai_routing::Tier::rate_micro` adds a per-tier cost rate (µ$/1k tokens, cheap<balanced<strong); every dispatched `ai_complete` charges `tier.cost_micro(est_tokens)` to a run-global meter, stamps the **real `cost_usd`** into the `ai_call` provenance (was hardcoded 0), and exposes the total via the `ai_cost_spent() -> i64` (µ$) builtin — the kernel counterpart to `llm_gateway.ax`. Deterministic (token estimate from prompt length) ⇒ works under mock. **LIVE-REQUEST TIER ROUTING DONE (was a real bug)**: `axon-ai` previously hardcoded `model:"claude-sonnet-4-6"` in the live request and ignored the resolved tier — so the provenance recorded the right model but the *actual call* always hit sonnet regardless of `strong`/`cheap`. Fixed: `ai_routing::Tier::api_model()` returns the wire model (`claude-haiku-4-5`/`claude-sonnet-4-6`/`claude-opus-4-8`, each **env-overridable** via `AXON_AI_MODEL_{CHEAP,BALANCED,STRONG}` for a proxy/gateway deployment), and the interp's live `ai_complete` now calls `axon_ai::complete_with_model(prompt, tier.api_model())` — so selecting `strong` (t3) actually routes to opus. Endpoint already env-configurable via `ANTHROPIC_BASE_URL` (Anthropic-Messages-compatible proxies/gateways work). Tests: `api_model_is_tier_specific_and_distinct`, `api_model_honors_env_override`. (Native codegen `__axon_ai_complete` still uses the sonnet default — threading the tier through the native ABI is a follow-on; the interp/asi-runtime path routes correctly.) Remaining: live token-count from the model response (today a length estimate); first-class `Budget` value type (Phase-7); native-codegen tier threading. | 📝 `governance/specs/R3-ai-primitive.md`, `R3b-named-tier-arg.md`, `R3c-ai-budget-meter.md` | Policy-gated fallback fires offline ✅ (E1300); **tier routing pins real model ✅ (live request now sends the tier's model + env override)**; **per-call `tier:` override ✅**; **AI call-budget enforced ✅** (E1301; per-fn `budget: N`); **per-token cost metered ✅** (real cost_usd + ai_cost_spent); **ai_call provenance recorded ✅** |
| **R4** | Three code zones: Static / Adaptive / Agent + compiler-enforced provenance | Three Code Zones; Error/Perf Logging | 100 | ✅ Strong | `@[adaptive]`/`@[experiment]`/`@[agent]` all behaviorally distinct now. `@[experiment]` injects tagged provenance (`zone:"experiment"`+label) but is excluded from `goal_run` best. **`@[agent]` MANDATORY ACTION-LOG DONE**: `call_builtin` injects an `event:"agent_action"` record (`zone:agent`, `action`=tool, `caps_used`=capability) whenever a capability-bearing builtin runs inside an `@[agent]` fn — compiler-injected, un-opt-out-able (I-13); non-agent fns doing the same call log nothing. JSONL carries real `event`/`zone`/`label`. **CODEGEN PROVENANCE TRIPWIRE DONE** (now R1 native builds): `scripts/provenance_parity.sh` + `codegen_provenance_matches_interp_on_adaptive_returns` compile one `@[adaptive]` program both ways and assert the native binary's return records carry the SAME discriminating fields (`event:"adaptive_return"`/`zone:"adaptive"`/`fn`/`score`) the interpreter writes. **Found+fixed a real I-13 codegen hole**: native `__axon_provenance_log_ret_{i64,f64}` was writing a degraded `event:"event"` record with no `zone` — fixed in `axon-rt/provenance.rs`. **NATIVE `input` FIELD DONE** (was a residual): native `adaptive_return` records now carry the structured `"input":<n>` field matching the interpreter (via F11's `__axon_provenance_log_ret_i64_in` → `log_adaptive_return(.., Some(input))`), not buried in the payload string. `goal_input_parity.sh` asserts `"score":84,"input":3`. **NATIVE `src` FIELD DONE** (closes the input/src residual): codegen emits `__axon_set_provenance_source(path)` in main's prologue (Codegen `source_path`, set from the build's source path); axon-rt stores it and `log_adaptive_return` stamps `"src":<path>`. Native adaptive records are now BYTE-IDENTICAL to the interpreter's (event/zone/payload/score/input/src), modulo ts_ms. **AGENT-ACTION CODEGEN PATH DONE (last residual closed)**: native codegen now injects the mandatory `agent_action` log too — codegen tracks `current_agent_fn` (set on `@[agent]` fn entry) and emits `__axon_log_agent_action(fn, action, caps)` at every capability-bearing builtin call inside it (I-13, un-opt-out-able); a non-agent fn logs nothing. Native records are byte-identical to interp (verified: `exec`→`exec`, `read_file`→`fs:read`). **R4 COMPLETE.** | `ROADMAP.md` §6–7; ✅ `governance/specs/R4-code-zones.md` (Reviewed) | Adaptive fn cannot compile without provenance injection ✅; `@[experiment]` distinct ✅; agent action log mandatory ✅ (cli_run: agent_actions_are_mandatorily_logged); **codegen↔interp provenance parity ✅** (adaptive byte-identical incl. input+src; **agent_action log too** via agent_action_parity.sh) |
| **R5** | Goal-directed optimization — `#[goal]` type, 3 strategies, deterministic test sets, eval hierarchy | Autonomous Optimization | 90 | ⚠️ Partial | 4 runtime strategies shipped (`goal_run`/`_continue`/`_random`/`_multistart`); **eval hierarchy STARTED**: `goal_eval(name, input)` does HELD-OUT evaluation — runs the metric on a held-out point WITHOUT recording it as a training probe (snapshot+restore, no overfitting/no provenance pollution), the train→holdout→target-gate primitive. **`#[goal(metric, target, max_evals, holdout)]` ATTRIBUTE SUGAR DONE**: auto-wires goal_run (train) + goal_eval (held-out) + a gate, injecting `goal_met` into the fn body; E1500/E1503/E1504 validation. **FIRST-CLASS GOAL VALUE (userland) DONE**: `examples/stdlib/goal.ax` — a `Goal` *value* you construct/store/pass/evaluate (the ROADMAP §6 `Goal<M>` shape at the userland layer, same two-track move as agent.ax/F12), bundling metric+target+Budget+a hard Constraint; `goal_observe` marks it met only when the target is reached AND the hard guard holds (a high score that breaks the guard is disqualified to 0 — safe optimization), `goal_step`/`goal_can_step` enforce the spend budget. 6 @[test] gated (cli_run `goal_stdlib_module_tests_pass`). **LIST-LITERAL `test_set: [...]` DONE**: the attr parser now accepts a bracket list (`parse_attr_atom` renders `[a,b,c]` → flat `"a,b,c"`); `#[goal(... test_set: [5,7,9])]` evaluates the metric on EVERY held-out point and gates on the WORST (min) score — so a fn cannot pass by overfitting one point; a non-integer element → E1503. Still missing: kernel `Goal<M>` value type with SMT-checked constraints + principal scoping (Phase-7). | `ROADMAP.md` §5–6; `spec/stdlib.md`; `examples/stdlib/goal.ax` | `#[goal(metric, test_set, target)]` runs to target on a held-out set ✅ (multi-point worst-case gate); provenance per experiment ✅; **first-class Goal value (userland) ✅**; `#[goal]` attr sugar + `test_set: [...]` ✅ |
| **R6** | Capability security — content-addressed imports, compile-time I/O restriction, AI audit on import | Supply Chain Security; Capability Permissions | 89 | ⚠️ Partial | `@[contained]` enforces net/fs/exec at fn boundary. **Import-edge boundary VERIFIED (I-11)**: static cap checker catches a `mod`+`use`-imported fn's `@[contained]`/`never:` violation (E1004) on the merged program. **Content-addressed lockfile WORKING end-to-end**: `lockfile.rs` (zero-dep `axh1:` SHA-256 + deterministic `axon.lock`) wired to CLI — `axon lock <file>` hashes each `use`d module + writes the lock; `axon verify-lock <file>` recomputes and rejects a tampered module with **E1201** (exit 2), a missing entry with **E1202**. The PRD's "tampered content hash rejected" is delivered. **Import-edge cap-widening DONE (E1203)**: a `@[contained]` importer that imports a module exercising a capability it doesn't grant is rejected at `axon check` (`check_import_capabilities`, opt-in: uncontained importers unaffected — back-compat). **Transitive-`use` hashing DONE**: `axon lock`/`verify-lock`/E1203 now cover the whole import closure (prog→mid→leaf all pinned; a tampered deep module is E1201). **AUDIT-ON-IMPORT DONE (E1204)**: `audit.rs` — `axon lock` audits each module's capability surface (`audit_module`) and pins a deterministic verdict (`clear`/`flagged`/`denied`) into `axon.lock` as `<verdict>:<hash>`; on every build the verdict is re-validated by hash (**no AI call at compile time**); a `denied` verdict blocks the build with E1204. **`exec` BUILTIN + EXEC CAPABILITY DONE (closes the deviation)**: a real `exec(cmd, [args]) -> Result<str,str>` builtin spawns a process via the `AxonHost` seam (DefaultHost = std::process; a sandboxed/browser host defaults to denied); `classify_call` maps it to `IoKind::Exec`, activating the previously-dormant `@[contained] exec: none|any` axis — `@[contained(exec: none)]` now rejects `exec` at check (E1001), `exec: any` allows it. The R6 audit now keys `denied` on undeclared **`exec` OR `net`** (both highest-risk surfaces), matching the spec instead of substituting net. **CODEGEN `exec` EMITTER DONE**: `exec` is no longer interp-only — native codegen emits it (`__axon_exec` in axon-rt: spawns the process, returns stdout/err via the ±len Ok/Err convention; codegen unpacks the `[str]` args array). Native==interp on both the Ok (stdout) and Err (message) paths (`scripts/exec_parity.sh`); previously native silently produced no output. Remaining: AI-audit *quality* layer (obfuscation reasoning via R3) is the documented follow-slice. | 📝 `governance/specs/R6-capability-security.md` (Reviewed) | Import without declared capability fails compile ✅ (E1004 + **E1203 import-edge**); **tampered content hash rejected ✅** (axon lock/verify-lock → E1201); **audit on import ✅** (E1204; exec OR net denied; deterministic verdict pinned + hash-revalidated); **exec capability live ✅** (exec builtin; E1001 on `exec: none`) |
| **R7** | Cross-platform targets — native / wasm / js / mobile from one source | Cross-Platform; Mobile; UI; 3D | 62 | ⚠️ Partial | **Slice A DONE — Axon runs on wasm.** The tree-walking interpreter compiles to `wasm32-wasip1` and runs `.ax` headless under `wasmtime` with **identical exit codes + stdout to native on 15/15 pure-compute examples** (same `interp.rs`, two targets — identical by construction, I-2; verified live this session). One host touchpoint changed: `on_deep_stack` runs on the single wasm stack (`#[cfg(wasm32)]`); 64 MiB wasm stack + `RECURSION_LIMIT=450` so deep recursion fails with the same graceful panic as native. **`axon target` CLI + AxonHost SEAM DONE**: host touchpoints route through a thread-local `AxonHost` (DefaultHost==std on native). **Slice B AOT-wasm OBJECT EMISSION DONE**: `axon target build --engine codegen --target wasm32` emits a real WebAssembly object via the inkwell wasm32 backend (`compile_to_wasm_object`), magic-verified `\0asm` (confirmed `file`-recognized 22 KB MVP wasm from math.ax) — the real IR→wasm codegen step, no longer an E0907 stub. **Slice B link FULLY DIAGNOSED end-to-end (2026-06-03)**: built `axon-rt` for `wasm32-wasip1` (succeeds, 11 MB staticlib), emitted the object for wasip1, and linked object + wasi `libc.a` + `crt1-command.o` + `libaxon_rt.a(wasm)` via `rust-lld -flavor wasm`. The link SUCCEEDS-with-warnings but the wasm **TRAPS under `wasmtime`** — `rust-lld` reports `function signature mismatch` on `__axon_str_*`/`__axon_parse_int_err` AND even `memcmp`/`write`/`strlen`/`snprintf`: codegen bakes **i64** ptr/len into the str/array IR (the AxonStr `{i64 len, i64 ptr}` ABI) while wasm32 needs **i32**. Even `21+21` drags all 19 `__axon_*` externs (`declare_builtins` emits the whole table). So the real Slice B blocker is a **wasm32 codegen ABI retarget (i64→i32 pointers + wasi entry + extern pruning)** — multi-slice codegen work, scoped in `R7-targets.md` §12 Q6, NOT a one-step link. The libc/sysroot half is solved (wasi `libc.a` + a building wasm axon-rt are on disk). **Slice A (interp→wasm, 15/15 parity) remains the shipped path satisfying the R7 "runs identically" acceptance by construction.** Remaining: the i64→i32 wasm ABI retarget, BrowserHost (wasm-bindgen), js/mobile. | 📝 `governance/specs/R7-targets.md` (Reviewed — Slice A + object-emit landed; Slice B link fully diagnosed = ABI retarget) | Same `.ax` runs on native and wasm with identical observable results ✅ (15/15 pure-compute, `scripts/wasm_parity.sh`); **AOT wasm object emitted from codegen ✅** (magic-verified); **AOT wasm link diagnosed: i64↔i32 ABI mismatch is the real blocker, not libc ✅** (empirically traps under wasmtime) |
| **R8** | Built-in testing + structured errors — `#[test]`, property-based `forall`, AI-parseable diagnostics | Testing; Error Messages | 100 | ✅ Strong | `@[test]`/`should_fail` + **`forall` property testing DONE**: `@[test] @[forall(n: N)]` randomizes typed params (i64/f64/bool/str) over N cases via seeded RNG, and on failure BINARY-SEARCH-SHRINKS to the minimal counterexample (`a < 50` → `a=50`) with a reproduce seed. **Versioned machine-stable diagnostic schema DONE** (`axon-diag/1`, `diag_schema.rs`): `axon check --json` emits structured `{schema,severity,code,message,help}` NDJSON (code as a first-class field, version-tagged). **TYPED END-TO-END DIAGNOSTIC DONE** (§10): `run_check_pipeline_located` resolves each typed error's byte-`Span` → (line,col) via `SourceMap` and returns located `PipelineDiagnostic`s; `PipelineDiagnostic::json()` emits `axon-diag/1` WITH `file`/`line`/`col` first-class; `axon check --json` now routes through it (E0102 mismatch → `"line":3,"col":5`). **STRUCTURED `expected`/`found`/`help` FIELDS DONE**: `PipelineDiagnostic` now carries `expected`/`found`/`help: Option<String>` (from the typed InferError/CheckError), and `json()` emits each as a discrete key when present — so a tool can branch on the type pair without re-parsing prose (`{...,"expected":"str","found":"i64"}`). Additive (consumers ignore unknown keys), so the schema stays `axon-diag/1`. **PARSE-ERROR LOCATION DONE** (last residual closed): `Parser::parse_program_located` captures the failing token's `current_span`; `parse_source_located` returns the byte offset; `cmd_check` resolves it to (line,col) and emits a located `PipelineDiagnostic` JSON — a parse error like `@` now reports `"line":2,"col":5` instead of span-less (lexer errors map to line 1 col 1, carrying their own position in the message). **R8 COMPLETE.** | `TESTING_STANDARD.md`; `spec/stdlib.md`; `governance/specs/R8-diagnostic-schema.md` | `forall(a,b){assert(f(a,b)==f(b,a))}` runs N cases ✅ + shrinks ✅; error JSON schema versioned ✅; **diagnostics carry source location end-to-end ✅** (incl. parse errors); **structured expected/found/help ✅** |
| **R9** | Layer-1/3 alignment — `Uncertain<T>`, `Temporal<T>`, `#[contained]`+`#[corrigible]`, `#[verify]` | ASI Extensions; Structural Alignment; Formal Verification | 80 | ⚠️ Partial | Uncertain/Temporal/`@[verify]`(runtime, composite preds)/`@[contained]` done. **UNCERTAIN<T> BINOP EXECUTION IN THE INTERPRETER DONE (was a real gap)**: codegen had `emit_binop_uncertain` but the interpreter's `eval_binop_vals` did not — so confidence-propagating code (`uncertain(10,0.9) + uncertain(5,0.8)`) could be compiled but PANICKED when interpreted (`axon run`). Fixed: `eval_binop_vals` now detects an `Uncertain` operand, operates on the inner values, and carries `min(confidence)` forward (a non-Uncertain side = confidence 1.0; comparisons yield `Uncertain<bool>`) — byte-identical to codegen (`uncertain_propagation.ax` runs the same native+interp, in the 29/29 parity sweep). `&&`/`||` over `Uncertain<bool>` route through the same path (no short-circuit when combining confidences). Tests: cli `uncertain_binops_propagate_minimum_confidence`. **`@[corrigible]` LATCHING KILL-SWITCH DONE**: `corrigible_halt()` trips a one-way latch (no resume builtin by design); once latched, every `@[corrigible]` fn call is refused before its body runs (engine-enforced, keyed on the annotation) — `corrigible_halted()` lets a loop wind down gracefully, an unguarded post-halt call fails closed with a distinct exit code 4 (`Flow::Halted`, distinct from panic 101 / verify 3 / static 2). The latch is targeted (non-corrigible fns still run). **SMT-backed `@[verify]` DONE** (`smt` feature, opt-in): `axon verify` encodes a fn's body as a Z3 term and PROVES the `value OP K` bound for all inputs (unsat negation) or reports a concrete counterexample (E1102, e.g. `dec(x)=x-1` violated at x=0); out-of-fragment (loops/calls) → W1103 (runtime gate applies). **FLOAT FRAGMENT DONE**: `f64` params/returns/non-integer bounds now route to a Z3 **Real** encoder (`prove_one_f64`/`encode_expr_real`) — `axon verify` PROVES float bounds (`x*x >= 0.0` proven for all real x; `x-0.5 >= 0.0` → counterexample) over straight-line `+`/`-`/`*`/`ite`. Honest caveat: Z3 Real is exact rational arithmetic, so it proves the *mathematical* bound (IEEE rounding/NaN/Inf out of fragment). Straight-line integer + float fragments done; loops/recursion-invariants remain (need invariant inference, Phase-N). Z3 isolated behind the feature — default build never links it (verified by the standard gate). | `spec/compiler-phase5.md` (SMT); `ROADMAP.md` §0,§7 | `@[corrigible]` kill-switch latches ✅; **`#[verify]` proven by Z3 on integer AND float bounds ✅** (smt: 8 unit; `axon verify` proves x*x>=0.0) |
| **R10** | Self-improving compiler — learned optimization passes graduated from AI-discovered asm | Self-Improving Compiler; Intelligence Maturity | 98 | ⚠️ Partial | **Verification harness + graduation firewall BUILT.** Four gates (`improve.rs`): G1 correctness (interpreter oracle, whole corpus, E1401; never AI — E1406 by construction), G2 capability-diff (I-12, E1402), G3 regression (`@[test]` outcome preserved, E1403 — catches what G1 misses), G4 perf (opt-in timing, advisory `faster` claim, W1410). Graduation (`manifest.rs` + `axon improve` CLI): `graduate` enforces multi-sig of ≥2 **distinct** root Principals (E1404 — a duplicate signer / self-graduation is refused); content-addressed `passes.manifest` (`axp1:`/`axv1:`/`axc1:`), malformed → E1405; a pass not in the manifest never runs; `list`/`revert` round-trip. **DISCOVERY (unprivileged proposal side) BUILT**: `axon improve discover <corpus>` scans the corpus for the canonical safe optimization — arithmetic-identity simplification (`x+0`/`0+x`/`x-0`/`x*1`/`1*x` → x) — via a deterministic read-only AST walk (`improve::discover_arith_identities`), and writes a `proposals/<name>.proposal` that **grants nothing** (pure data: name/description/opportunity-count). Only `verify` (four gates) then a multi-sig `graduate` can make a pass runnable — the I-12 firewall (discovery proposes, humans dispose) holds by construction. A clean corpus proposes nothing (no vacuous pass). **REAL REWRITE PASS BUILT + VERIFIED**: `improve::fold_arith_identities_pass` is the executable counterpart — a pure `Program→Program` rewrite that folds `x+0`/`1*x`/etc. to `x` recursively (conservative: only collapses provable no-ops, returns unlisted variants unchanged). `axon improve verify --pass fold-arith-identities` runs the ACTUAL rewrite through the four gates and PASSES G1 (the interpreter oracle confirms behavior is preserved) + G2 (no new capability) over **all 28 examples** — a real, proven-correct optimization, not the identity baseline. The discover→verify→graduate pipeline is now end-to-end with a real pass. **AI-DRIVEN DISCOVERER BUILT (the PRD headline `axon improve discover --ai`)** — discovery is AI-driven WITHOUT letting AI weaken the TCB: the AI selects template NAMES from a **closed compile-time registry** (`improve_templates::TEMPLATES`, `const [(name, fn-ptr)]`); it NEVER authors code. An unknown name → **E1407** before a proposal is written (never reaches verify); a graduated name absent from the registry → **E1408** (tamper/skew). The selected name maps to the SAME deterministic Rust pass the four gates verify, so an AI selecting a (hypothetical) miscompiling template still fails G1 — the firewall is unchanged, AI cannot route around the interpreter oracle (I-2/I-12 hold by construction). Discovery origin (`proposed_by: static|mock|ai:<model>`) round-trips Proposal→manifest so a multi-sig reviewer sees AI provenance. Deterministic under `AXON_AI_MOCK=1`. A 3-lens design + **5-attack adversarial red-team (multi-agent workflow)** preceded the build; 2 real holes (TOCTOU on name validation, provenance-audit gap) were closed by construction. Remaining: live-model wiring (the mock path ships; validate-against-registry + four-gate firewall is identical for a live model) + growing the template menu (each a reviewed commit). | 📝 `governance/specs/R10-self-improving-compiler.md` (Reviewed) | A discovered pattern verified correct (G1 ✅) + safe (G2 ✅) + non-regressing (G3 ✅) + faster (G4 ✅), graduation gated by multi-sig (E1404 ✅); **discovery proposes candidates ✅**; **real rewrite pass verified correct+safe ✅** (fold-arith-identities through the gates); **AI-driven discoverer ✅** (closed-registry selection, E1407/E1408, proposed_by provenance, mock-deterministic, red-team-hardened) |

**Weighted completion:** language-core ≈ 55% · by-PRD-phase ≈ 30% · full-platform vision ≈ 12%.

---

## Reading the gaps as a work queue

Ordered by **(load-bearing × cheapness-to-close)** — the autonomous builder's default priority:

1. **R8 `forall` property testing** — cheap, unblocks deeper testing of *everything else*. Do first.
2. **R5 `#[goal]` first-class** — the autonomy engine is closest to complete; finishing it compounds.
3. **R9 `#[corrigible]`** — small surface, large safety payoff; pairs with existing `@[contained]`.
4. **R3 AI routing + `#[ai(policy)]`** — the differentiation; needs a spec first (forks on reproducibility).
5. **R6 capability/registry** — security gate; spec-first, because under-spec = exfiltration risk.
6. **R1 native build** — high value, high risk; the fix is prototyped but unvalidated. Schedule a focused effort, not a tick.
7. **R7 targets / R10 self-improving** — largest, most speculative; spec now, build after R1 lands.

---

## Update protocol

- Any commit that changes a requirement's truth **must** update its `%`, `Status`, and `Gap`.
- A requirement moving to ✅ DONE **must** cite the passing acceptance test in the commit body.
- New PRD features get a row here *before* implementation — if it's not in the matrix, it's not a tracked requirement.
- The weighted-completion line is recomputed whenever a `%` changes.
