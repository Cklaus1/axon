# Tech Specs — Index

Specs for the unbuilt high-risk requirements (`../REQUIREMENTS.md`). Each is **spec-first** because it has
an architectural fork that is cheap on paper and expensive once merged. Until a spec here reaches
**Reviewed**, do not write implementation code for that requirement (`../BUILD_PROTOCOL.md` Gate 1).

Each stub below states the **decisive fork** that the full spec must resolve first — that's the question
that gates everything else. Fill against `../SPEC_TEMPLATE.md`.

| Spec | Requirement | Status | The decisive fork |
|---|---|---|---|
| `R1-codegen-build-unblock.md` | R1 — native build (the stall) | 📝 Draft | **How to get past the never-finishing codegen build, after every cheap fix was tried + falsified?** *Resolved:* the one untried structural lever — **move the ~158 hand-emitted inline-IR builtins out of `codegen/builtins.rs` into the `axon-rt` staticlib as ordinary `extern "C"` Rust**, exactly like the 38 builtins (channels/sqrt/file-IO) that are *already* extern and cost ~0 IR. This deletes IR *volume* (an asymptote fix) where the shipped `#[inline(never)]` wrappers only cut a constant factor. Differential-parity vs the interpreter oracle (I-2) — provable NOW; the build-finishes gate needs a real machine (Q1/Q2). New E16xx band. **Human-driven, not a loop tick.** |
| `R1b-str-return-abi.md` | R1 — str-returning builtins (deferred from Batch 2) | 📝 Draft | **Option A: by-value `#[repr(C)] AxonStr` return vs Option B: out-params (`*mut i64`, `*mut *mut u8`).** *Resolved:* **Option B wins** — its ABI is proven by 14 existing str-return externs (`__axon_read_file`, `__axon_read_line`, `__axon_i64_to_str_radix`, `ai_extract_*`); Option A's by-value struct return has zero axon-rt precedent and cannot be proven safe without the native build (§12 Q1). The call-site glue (~7 LLVM IR instructions per builtin) is applied pattern (already used for 38 externs), not new codegen logic. **Drift on `str_reverse`/`str_replace` RESOLVED (§12 Q2/Q3):** per I-2 the interpreter is canonical (char-reverse; Rust `replace`), the ports match it, and codegen's divergent inline bodies are fixed-by-deletion in the migration — drift-collapse, not drift-freeze. Logged as findings #38/#39. `str_repeat`/`str_slice` shipped (`d9dd83f`); `str_reverse`/`str_replace` now unblocked. |
| `R1d-single-source-builtins.md` | R1 — builtin authoring: one edit, not four | 📋 Draft | **How does a builtin stop being defined in 3–4 hand-synced places (sig table + interp arm + codegen IR/extern + return-type insert)?** *Resolved:* **(b) a declarative `BUILTIN_EXTERNS` registry** codegen iterates to declare every extern, paired with the axon-rt Rust impl — plain data matching the existing `const BUILTINS` style, no proc-macro. Collapses ~120 hand-written `declare_builtins` blocks into table rows + a 15-line loop; composes with R1 (the build-speed fix) since a registry row + axon-rt fn has ~0 IR. Bespoke call-site builtins (`to_str`, arr_*/dict_* loops) keep their special-cases. Slice 1 is a pure refactor gated by the full parity suite. |
| `R1e-ir-backend-consolidation.md` | R1 — one IR-emission path (retire the dead `IR`-trait shim) | 📋 Draft | **Codegen has THREE ways to emit the same LLVM IR — which survives?** (1) `build_wrappers::w_*` (1787 sites, the `#[inline(never)]` build-speed win), (2) direct `self.ir.builder.build_*` (154 sites, all in `expr.rs`), (3) the `IR`-trait arena shim (`ir.rs` + `impl IR for InkwellBackend`, **0 real callers**). *Resolved:* **(b) adopt `build_wrappers`, delete the dead trait.** The trait migration was **abandoned, not half-done**: it has 0 callers (`#![allow(dead_code)]`), its impl is full of `IRValue(0)` sentinel stubs (`ty_of`/`get_function`/`phi_add_incoming` non-functional), and `build_wrappers.rs:22` records the trait "did NOT help" and was *replaced* by the wrappers. IR.4 is **already done by accident** — the `Codegen` struct has NO bare `context/module/builder` fields (only `self.ir`), and the `MIGRATION.md` the comments cite **does not exist**. Wrappers stay (R1 resolved the *serde* axis, not the inkwell-monomorphization axis the wrappers address). Slices: delete `ir.rs`+the impl (−~600 dead LoC), converge the 154 `expr.rs` stragglers onto `w_*`, fix the stale mod.rs comments, add a drift tripwire. Pure refactor, gated by the parity suite. |
| `R1c-dict-runtime.md` | R1 — `dict_*` native codegen (the last large gap) | 📋 Draft | **How does a dynamically-typed `Dict` (interp's `Rc<RefCell<BTreeMap<String,Value>>>`) become native code when codegen values are statically typed?** *Resolved:* **(a) tagged-value runtime** — one `__axon_dict_*` extern family in axon-rt backed by `HashMap<String, {tag:i64, payload:i64}>`, mirroring the channel `__axon_chan_*` precedent (opaque `*mut` handle, C-ABI externs, NOT inline IR). Monomorphized typed dicts (option b) rejected because the real examples MIX value types in one dict. 4 sliced commits; until slice 1 lands, `dict_*` stays honestly E0910-gated (interp runs it today, I-2). `arr_group_by` (returns a Dict) is gated on this. |
| `R2a-type-map-threading.md` | R2 — stop re-deriving types (front-end) | 📋 Draft | **How does the back-end learn an expression's type — re-derive it, or read what HM already computed?** *Resolved:* **(a) persist + thread a `HashMap<ExprId,Type>`.** HM inference (`infer.rs`) currently throws its result away (`let _subst = …` at `lib.rs:861`/`main.rs:2560`); expression types are then re-derived THREE times — authoritatively in infer.rs, again in `checker.rs::check_expr`'s private scope, and a third time by codegen's 162-line `infer_expr_sem_type` heuristic (`mod.rs:1113`) that every new collection builtin must be patched into. Persist the node→type map and have codegen/checker LOOK UP. Sub-fork (the real gate): a stable key — **(a2) a `u32 Expr.id` from a post-desugar numbering pass** over span-as-key (leaky on synthesized nodes). Deletes the heuristic + `local_types`, collapses 3 derivations to 1, kills the codegen-disagrees-with-HM bug class. Pure refactor, gated by `parity_all.sh` + a migration-phase disagreement assertion. |
| `R1f-differential-parity-fuzz.md` | R1 / I-2 — auto-find engine divergence | ✅ Landed (slices 1+2+2b) | **What substrate runs the codegen side of the comparison — per-input AOT, JIT, or one batch program per builtin?** *Resolved:* **(b) one generated `.ax` per builtin looping over many embedded inputs, compiled once via the real AOT pipeline**, diffed against the interpreter. `scripts/fuzz_parity.sh`: 30 random descriptors (i64/pos/f64/str domains) + 4 NaN/inf (`nan_case`, compare:Stdout) + 4 overflow (`expect_overflow`, compare:ExitCode — asserts the documented checked-vs-wrapping CONTRACT, not equality). **Earned its keep immediately — found 3 real `to_str` divergences the 21 fixed-case harnesses missed**: `-0.0`→"-0", sci-notation `1.00000e6`-vs-`1e+06`, negative-NaN→"-nan" (all fixed; interp `fmt_g` converged onto C `%.6g`). Gated via a cli_run test + `parity_all.sh`. Remaining: R1f-2 (interp-calls-axon-rt collapse, separate spec) + v2 collection/ASI `Value`-level generators. |
| `R0-interp-module-split.md` | — maintainability (the other engine) | 📋 Draft | **True module split mirroring `codegen/`, or inline section banners?** *Resolved:* **(a) an `interp/` directory.** `interp.rs` is 6,749 lines in one flat module — the lone monolith of the two engines (codegen is already a 13-file dir). `call_builtin` (37%) + the goal optimizer (16%) are >half the file and cleanly separable. Split into `mod/eval/builtins/asi/goal/provenance/value.rs`; sub-fork = promote the per-arm `ok!`/`want` boilerplate to a shared macro+fn when the giant match is cut across category routers. Pure code-move, zero behavior change → the in-file tests + `parity_all.sh` are the net. 6 independently-committable slices, `provenance.rs`/`value.rs` (mostly free functions) first. Enables a future shared formatting crate (the real `%.6g`/overflow drift fix). |
| `R4-code-zones.md` | R4 — code zones + provenance | ✅ Reviewed | **How is provenance made un-opt-out-able (I-13), engine-uniform?** *Resolved:* injection is a compiler pass keyed on the zone annotation (as `interp.rs:701` already does for `@[adaptive]`), shared by both engines so neither can skip it; there is no opt-out construct (rejected by design). `@[experiment]` becomes behaviorally distinct (tagged, excluded from `goal_run` best) instead of a no-op synonym; `@[agent]` gets mandatory action logging. Interpreter slice testable now; codegen conformance is an `#[ignore]`d parity tripwire until R1. New E15xx band; completes I-13. |
| `R3-ai-primitive.md` | R3 — AI as primitive | ✅ Reviewed | **Reproducibility vs. capability.** *Resolved in the draft:* YES — replay requires it, so every AI call pins `(model, version, params_hash, prompt_hash, mode, cost)` into an `event:"ai_call"` provenance record (schema settled first, §4.3); routing (tier→model) and `@[ai(policy)]` layer on top. Live determinism is not faked — delivered via mock + hash-keyed replay (R9). Cost-optimal routing waits on Phase-7 `Budget` (§12 Q2). |
| `R6-capability-security.md` | R6 — capability/registry security | ✅ Reviewed | **Content-addressing model.** *Resolved in the draft:* `axh1:`-tagged SHA-256 over raw source bytes (not AST); committed TOML `axon.lock` (name→hash→bytes, `--locked` CI mode); AI-audit runs at **acquisition** (`axon add`/`lock`), verdict pinned in the lockfile, re-validated by hash on compile (no AI call at build). Static capability checker (E1001–E1004) stays the hard I-11 gate; the audit is defense-in-depth, not the sole boundary. New E12xx band; import-edge cap-widening = E1203. |
| `R7-targets.md` | R7 — cross-platform targets | ✅ Reviewed | **Does wasm reuse the LLVM backend or need a separate codegen?** *Resolved: the fork was a false binary.* A **third option** unblocks R7 now — compile the pure-Rust tree-walking **interpreter** (`interp.rs`, no LLVM) to `wasm32`, running `.ax` in-browser with **identical results by construction** (I-2). Real work = abstracting the ~6 host touchpoints (fs/env/thread/sleep) behind an `AxonHost` trait. AOT-wasm via LLVM (option B) is **unblocked now that R1 landed** (2026-06-01, `../../BUILD_RESOLVED.md`) but deferred by priority; a lean wasm backend (C) is last-resort. New E0907/E0908/W0910; Slice A is deliverable, Slice B unblocked. |
| `R11-capability-minting.md` | Phase-7 — runtime `capability_minter` (I-12) | ✅ Reviewed | **Attenuation enforced *by construction* vs *checked after the fact*?** *Resolved: by construction.* A minted sub-Principal's caps = `want_X && parent.X` and its budget is carved (clamped to parent remaining, debited) — escalation is *unrepresentable*, not rejected-at-runtime, because a post-hoc checker is a second mechanism that can be bypassed (the I-12 failure mode). Mirrors R4's "no opt-out construct" + R10's G2 cap-diff. Userland slice landed (`examples/stdlib/principal_mint.ax`, 8 @[test]); kernel `Principal<Caps>` with SMT-proven subset relation (E1210) is the Phase-7 follow-on. R6 covers the *import* edge; this is the *runtime delegation* edge. |
| `R10-self-improving-compiler.md` | R10 — self-improving compiler | ✅ Reviewed | **Verification of a graduated pattern.** *Resolved:* a four-gate harness — G1 correctness (the **interpreter is the equivalence oracle**, I-2: `interp(P(c))==interp(c)` over the whole content-addressed corpus, never an AI judgment), G2 capability-diff (I-12, E1402), G3 full-suite regression, G4 perf. **All four gates verifiable now** — R1 landed (2026-06-01, `../../BUILD_RESOLVED.md`), so G4 (measurably faster) needs only a timing harness, not the native build. Graduation needs multi-sig of root Principals (the compiler can't graduate its own passes) + boot attestation. New E14xx band; proposes I-17. |
| `R9b-smt-loop-invariants.md` | R9 — SMT `@[verify]` over loops | 📝 Draft | **Invariant inference strategy: template/Houdini vs abstract-interp vs CHC/PDR?** *Resolved: template-driven (octagon-shaped) candidates discharged by the existing R9 Z3 encoder, falling through to Z3 CHC (`fixedpoint`/Spacer) when no template fits — never a separate abstract-interp analyzer (a second model = I-2 drift, cf. R10).* The invariant is a Z3-checked *witness*, never trusted, so weak heuristics yield `Unsupported`, never a false proof. Widens the proven fragment from straight-line (R9 shipped) to single linear `i64` loops. Reuses E1102/W1103; entirely under `--features smt`. |
| `R12-kernel-runtime-services.md` | Phase-7 — kernel runtime tier | 📝 Draft | **Preemptive scheduler vs cooperative over the interpreter's existing spawn model?** *Resolved: cooperative fiber run-queue on the shipped spawn/channel machinery* (a preemptive model = a second execution model to keep in sync with `interp.rs`, I-2 risk; ASI workloads are I/O-bound not compute-preempted). Sequences the four userland TCB modules' KERNEL counterparts into 5 gated slices: principal_authority → cooperative_scheduler → live supervisor_root → durable Store → kernel Goal/LLM (real token-count, closes R3's last gap). New E16xx band. The substrate (slices 1–2) is the prerequisite the rest hang off. |
| `R13-native-ffi.md` | R13 (new) — native FFI; gates UI/GPU/mobile/3D | 📝 Draft (⚠ changes I-11/I-12) | **Raw C `extern` in user Axon vs curated, dual-compiled native modules?** *Resolved: native modules.* A **native module** is a Rust shim crate compiled into BOTH the native link (codegen) AND the compiler process (interpreter) — one impl, two engines. **Preserves I-2 (modulo the marshalling layer, fuzzed + trace-checked), I-5 (resource handles are affine `own` → native use-after-free is a *compile* error), I-4 (unforgeable indexed handles).** **CHANGES I-11 and I-12** (this spec doubles as the invariant-change proposal): native code runs with ambient host authority the capability boundary can't enforce *into*, so I-11 drops from *total* to *edge-enforced + trusted-within*, mitigated by object-capability-passing (caps handed in, not ambient) + attenuated R11-minted grants + later out-of-process sandbox; I-12 changes because native modules ARE new TCB (gated, content-addressed, R10's G2 must fail on a new `use native::`). Carries a mandatory **TCB-delta ledger** (wgpu/winit/vello = ~100k+ trusted LoC). New E17xx band. **Critical path is R13 → Phase-6 `resume` runtime → R-UI**, not R13 → R-UI: a static frame needs only R13; any interactive app needs the unbuilt continuation runtime. |
| `R7c-browser-host.md` | R7 — browser target (`unknown-unknown` + WebGPU) | 📝 Draft | **Reuse the wasip1 object, or a new path?** *Resolved: same codegen object, new link mode + a `BrowserHost`.* The i64→i32 + size_t AOT-wasm bridge already landed (R7-targets §12, 2026-06-03), but targets `wasm32-wasip1`/`wasmtime` — headless, not a browser. Builds the `AxonHost` impl R7b deferred: `wasm32-unknown-unknown` + wasm-bindgen glue, virtual fs/env over JS, `performance.now`, WebGPU `<canvas>`. No `.ax` surface change. New E0911/E0912/W0913. **Honest tiering: v1 = compute-only** (the browser is async + single-threaded, Axon is neither — sync `run()` freezes the tab); **interactive/async-I/O (fetch, frame loop, net, threads) is gated on the Phase-6 `resume` runtime**, same dependency as R13/R14. |
| `R14-mobile-targets.md` | R7 — Android (now) / iOS (needs macOS host) | 📝 Draft | **New codegen backend, or toolchain + wrapper?** *Resolved: toolchain + wrapper — but with two caveats the first draft hid.* R1 already emits AArch64; `emit_object_and_link` takes an arbitrary triple (`link.rs:65`); a per-triple cross-linker is read from `cross.toml` (`link.rs:217`). So **no codegen path, no linker mechanism** is added — *for compute*. **(1) iOS ≠ Android:** Android cross-compiles from Linux now; iOS needs Apple's macOS-only toolchain and is build-blocked on this host — v1 is Android-first. **(2) the "~50-line wrapper / mostly toolchain" claim holds only for compute:** a real app's run loop is OS-owned (`UIApplicationMain`/`Activity`), so the wrapper is a hand-written `AxonRuntime` *lifecycle bridge* (TCB) + a ~10-line generated shim, and the bridge is **gated on the Phase-6 `resume` runtime**. Critical path: **R14 (Android compute) → Phase-6 resume → R-UI**, iOS branching off at a macOS host. New E17xx codes (shared with R13). |
| `R15-resume-runtime.md` | Phase-6 — suspend-across-host-event runtime; the SHARED blocker for R7c interactive / R13 / R14 UI | 📝 Draft | **How is a suspended computation preserved across a host event — replay, CPS, or a stackful coroutine?** *Resolved: stackful coroutine, interpreter-only.* The shipped Phase-6 replay `resume` (`7a79772`) re-runs a `with`-body from a snapshot — sound for handler semantics but it **re-fires every pre-suspend side effect on each resume** (a frame loop would reprint its history each frame), so replay-at-program-scope is REJECTED for interactive I/O (confirmed by reading `replay_continuation`). CPS is too invasive (rewrites every eval form). A **stackful coroutine** (v1: an OS thread parked on a channel; a coroutine crate later) runs the UNCHANGED recursive `eval` and parks/unparks at `host_await` — preserving all semantics by construction (I-2), mapping onto the shipped kernel fiber model (which needs a `Suspended(token)` state). Native/wasm codegen **E0910/E1315-refuses** `host_await` (sound-by-refusal, like handlers today); the browser binding (single-threaded → Asyncify or a JS step-loop) and the `on_frame`/`fetch` surface are follow-on slices. Interp-only + additive → zero-overhead on the non-suspending path. New **E1315** (E131x band). **THIS IS THE CRITICAL-PATH ITEM** for all interactive targets. |

---

## Build order (from `../REQUIREMENTS.md` work queue)

These four are **not** the next things to build. The cheap-and-compounding work (R8 `forall`, finishing
R5 `#[goal]`, R9 `#[corrigible]`, and the confirmed bug-hunt fixes — overflow/seed/analytics) comes first.
These specs exist now so that *when* their requirement reaches the front of the queue, the architectural
fork is already resolved on paper rather than discovered mid-implementation.

**Recommended sequencing:**
1. Land the confirmed bug-hunt fixes (`../BUG_HUNT_2026-05-31.md`: #6, #19, #11, #4, #5) — they protect the
   success signal everything else depends on.
2. R8 `forall` → unblocks deeper property testing of all future work.
3. R5 `#[goal]` first-class + R9 `#[corrigible]` → finish the autonomy + safety surfaces that are closest.
4. **R3 spec → build** (the differentiation).
5. **R6 spec → build** (security gate for autonomous dep use).
6. **R1 native build** (focused effort, not a tick) → unblocks **R7** and **R10**.
7. **R7 spec → build**, then **R10 spec → build** (the recursive-improvement flywheel, last and most carefully).
